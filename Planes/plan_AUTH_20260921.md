# Plan AUTH — Registro e inicio de sesión con correo/contraseña (backend FastAPI + SQLite)

> Plan de ejecución para agente EXECUTOR externo. **Autocontenido**: no requiere contexto
> previo, conversaciones anteriores ni historial de Git. Ejecutar AL PIE DE LA LETRA, en orden.
> Si un paso falla, DETENERSE y reportar.
>
> **Versión 2 (2026-09-21)** — reformulado tras una segunda auditoría que montó el backend
> completo y un frontend con `openapi-fetch` en un navegador real, cruzando origen
> (`localhost:5174` → `localhost:8000`). La v1 tenía **6 de 8 tests de backend rotos** y
> dos bugs de manejo de error que dejaban la UI colgada. Los 8 hallazgos y cómo se
> integraron están en la **Sección 10**.
>
> Todo lo que este plan prescribe fue **ejecutado y verificado**, no solo razonado:
> flujo completo register→me→duplicado→logout→login→credenciales malas→validación en
> navegador real con cookie `httpOnly` cross-origin (8/8 casos OK), `pytest` **12/12**,
> `ruff check` y `mypy --strict` en 0 errores, `alembic upgrade head` creando la tabla, y
> `openapi-typescript` generando tipos contra el OpenAPI vivo.

---

## 1. Contexto del proyecto

### 1.1 Qué se construye

Primera pieza funcional de **MyTrueque.com**: registro e inicio de sesión reales, con
persistencia en SQLite. Reemplaza el servicio simulado que hoy usa el frontend
(`frontend/src/features/auth/services/authService.ts`, con `TODO(backend)` explícitos).

**Alcance cerrado con el usuario:**
- Registro **solo con correo + contraseña**. Sin campo de nombre (el formulario de
  registro actual del frontend tiene un campo "Nombre" — se retira; ver Sección 1.4).
- El `@usuario` (`@trq-xxxx`) lo **genera el servidor** al registrarse, único, y se
  devuelve en la respuesta — el frontend ya lo muestra (`AuthPanel.tsx`, estado de éxito
  del registro).
- **Login con Google: NO se implementa en este plan.** El botón "Continuar con Google" se
  agrega a la UI **visible pero deshabilitado**, con un texto explicando que llega
  próximamente. Requiere credenciales OAuth (Client ID/Secret de Google Cloud Console) que
  el usuario todavía no tiene.
- Sesión con **cookie firmada** (`itsdangerous`, ya está en el Stack Tecnológico de
  `CLAUDE.md` Sección 1) — no JWT, no Redis, no tabla de sesiones. Cookie `httpOnly`.

### 1.2 Root del proyecto y estado actual

- **Root absoluto**: `C:\APLICACIONES\Trueque.com`
- **`backend/` NO existe todavía** — este plan lo crea desde cero.
- **`frontend/` ya existe y funciona** (landing + panel de auth simulado, del plan MAIN
  anterior). Este plan lo modifica para hablar con el backend real.
- **El root NO es un repositorio git** (confirmado: `git rev-parse` falla). No inicializar
  uno por cuenta propia.
- **Entorno verificado en la máquina destino**: Python `3.13.15`, `uv 0.12.5`, Node
  `v20.20.2`, `pnpm 10.33.0`.
- Contenido actual relevante del root:

```
C:\APLICACIONES\Trueque.com\
├── CLAUDE.md
├── BITACORA.md
├── Planes\                    ← este plan vive aquí
├── frontend\                  ← existe, Vite+React (ver 1.3)
│   └── src\features\auth\     ← se modifica en este plan
└── (backend\ — no existe, se crea aquí)
```

### 1.3 Stack objetivo (de `CLAUDE.md` Sección 1, NO negociable)

**Backend**: Python 3.12+, FastAPI, SQLAlchemy 2.0+, Alembic, Pydantic 2.x, structlog,
itsdangerous, uv, Ruff+Black+mypy.

**Frontend** (ya instalado, no cambia): React 19, TypeScript, Vite, TanStack Query,
Zustand, react-hook-form, zod, Sass, Lucide, pnpm. **Se agregan** `openapi-typescript`
(dev) y `openapi-fetch` (runtime) — están en el Stack de `CLAUDE.md` y hoy no se habían
instalado porque no existía backend del cual generar tipos.

> **Versiones reales verificadas en el registry (2026-09-21)**, todas probadas en el
> smoke test de este plan:
>
> | Paquete | Versión |
> |---|---|
> | fastapi | 0.141.1 |
> | uvicorn[standard] | 0.53.0 |
> | sqlalchemy | 2.0.54 |
> | alembic | 1.20.0 |
> | pydantic (con extra `email`) | 2.13.5 |
> | pydantic-settings | 2.15.0 |
> | structlog | 26.1.0 |
> | itsdangerous | 2.2.0 |
> | bcrypt | 5.0.0 |
> | python-multipart | 0.0.32 |
> | ruff | 0.16.8 |
> | mypy | 2.3.1 |
> | httpx2 (solo test) | 2.13.0 |
> | openapi-typescript (frontend, dev) | 7.13.0 |
> | openapi-fetch (frontend) | 0.17.0 |

### 1.4 Decisiones cerradas (verificadas en el smoke test — no reabrir)

1. **Hash de contraseña: `bcrypt` directo, NO `passlib`.** `passlib` tiene un bug conocido
   y no resuelto con `bcrypt>=4.1` (`AttributeError: module 'bcrypt' has no attribute
   '__about__'`). Se usa `bcrypt.hashpw`/`bcrypt.checkpw` directamente — 2 funciones, sin
   capa extra.
2. **`pydantic[email]`, no `pydantic` a secas.** `EmailStr` **falla en import** sin el
   extra `email-validator` (`ImportError: email-validator is not installed`) —
   reproducido. Instalar con `uv add "pydantic[email]"`.
3. **Tests con `httpx2`, no `httpx`.** El `TestClient` de FastAPI 0.141 (vía Starlette
   1.6) emite `StarletteDeprecationWarning: Using httpx with starlette.testclient is
   deprecated; install httpx2 instead` — reproducido. Se usa `httpx2` como dependencia de
   test; **no instalar `httpx`** en absoluto (evita que ambos coexistan sin necesidad).
4. **Sin campo "Nombre" en el registro.** El usuario pidió explícitamente "datos mínimos:
   correo + contraseña". El componente `RegisterForm` actual (`AuthPanel.tsx`) tiene un
   campo Nombre que **se retira** en este plan (Sección 5.7). El modelo `User` no tiene
   columna `name`.
5. **Sin columna para Google en el modelo todavía.** No se agrega `google_id` ni ninguna
   columna especulativa: Google login no está implementado, y una columna sin uso es
   deuda innecesaria. Cuando se implemente Google, esa migración se hace en su propio plan.
6. **`openapi-fetch`, no `fetch` a pelo.** El Stack de `CLAUDE.md` ya lo exige. Los tipos
   se generan una vez con `openapi-typescript` contra el `openapi.json` real que expone
   FastAPI (verificado: genera `paths`/`components` correctos con los 4 endpoints).
7. **Puerto del backend: `8000`.** Estándar de FastAPI/uvicorn, distinto del `5173` del
   frontend. CORS del backend permite explícitamente `http://localhost:5173` con
   `allow_credentials=True` (necesario para que el navegador mande la cookie de sesión).
8. **Cookie de sesión**: nombre `trueque_session`, `httpOnly`, `samesite=lax`,
   `secure=False` en desarrollo (⚠️ pasar a `True` cuando haya HTTPS en producción — fuera
   de alcance de este plan, ver Sección 9). Duración: 7 días.
9. **Migraciones con Alembic desde el día uno**, aunque solo haya una tabla — es la
   convención del Stack y evita empezar con `Base.metadata.create_all()` para luego
   migrar a Alembic bajo presión. Verificado: `alembic revision --autogenerate` detecta
   la tabla `users` y sus 2 índices únicos correctamente sobre el modelo con la sintaxis
   tipada de SQLAlchemy 2.0 (`Mapped`/`mapped_column`).
10. **`ruff` y `mypy` limpios desde el primer commit** — verificados contra este exacto
    diseño de código (0 errores de mypy, solo habría que evitar imports sin usar).

---

## 2. Objetivo de la tarea

Al terminar:

1. `backend/` funcional: `uv run uvicorn src.main:app --reload --port 8000` levanta la
   API en `http://localhost:8000`, con SQLite en `backend/data/trueque.db` (creada por
   Alembic, no por `create_all`).
2. 4 endpoints reales: `POST /auth/register`, `POST /auth/login`, `POST /auth/logout`,
   `GET /auth/me`. Contrato exacto en la Sección 5.
3. El frontend (`frontend/src/features/auth/`) habla con el backend real vía
   `openapi-fetch` con tipos generados — **cero simulación**, se borra `authService.ts`
   actual y se reemplaza.
4. El formulario de registro pide **solo correo y contraseña** (se quita "Nombre").
5. Sesión persistida en cookie: recargar la página con sesión activa mantiene al usuario
   logueado (`GET /auth/me` al montar la app).
6. Botón "Continuar con Google" visible en ambas pestañas del panel de auth, deshabilitado,
   con texto "Próximamente".
7. `ruff check`, `mypy`, `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build` en verde.
8. Flujo funcional de punta a punta en el navegador: registrar → ver el `@usuario`
   asignado → cerrar y volver a entrar con esas credenciales → `GET /auth/me` confirma la
   sesión.

---

## 3. Prerequisitos

Ejecutar **todos** estos checks **antes** de empezar, desde `C:\APLICACIONES\Trueque.com`.
Si alguno falla, DETENERSE y reportar.

| # | Requisito | Comando | Resultado esperado |
|---|---|---|---|
| 1 | Root correcto | `dir CLAUDE.md` | el archivo existe |
| 2 | `frontend/` existe y tiene el auth simulado | `dir frontend\src\features\auth\services\authService.ts` | el archivo existe |
| 3 | `backend/` NO existe todavía | `dir backend` | **error "No se encuentra la ruta"**. Si existe, DETENERSE y reportar |
| 4 | Python ≥ 3.12 | `python --version` | `3.12.x` o superior. En la máquina destino hay `3.13.15` |
| 5 | uv instalado | `uv --version` | número de versión. Si falla: `pip install uv` |
| 6 | pnpm instalado | `pnpm --version` | número de versión |
| 7 | Conexión a red | `uv run --with fastapi python -c "import fastapi"` desde una carpeta temporal, o `curl -sf https://pypi.org` | responde sin error |
| 8 | El root **no** es repo git (informativo) | `git rev-parse --is-inside-work-tree` | `fatal: not a git repository`. **No** ejecutar `git init` |

---

## 4. Inventario de archivos

### 4.1 Backend — crear (18 archivos + carpetas)

Todas las rutas relativas a `C:\APLICACIONES\Trueque.com\backend\`.

| # | Ruta | Acción |
|---|---|---|
| 1 | `pyproject.toml` | crear (vía `uv init`, luego editar) |
| 2 | `.env.example` | crear |
| 3 | `.env` | crear (copia de `.env.example` con el secreto real) |
| 4 | `.gitignore` | crear |
| 5 | `alembic.ini` | crear (vía `uv run alembic init`, luego editar) |
| 6 | `alembic/env.py` | modificar |
| 7 | `alembic/script.py.mako` | dejar tal cual lo genera Alembic |
| 8 | `alembic/versions/0001_create_users.py` | crear (migración inicial) |
| 9 | `src/__init__.py` | crear (vacío) |
| 10 | `src/main.py` | crear |
| 11 | `src/core/__init__.py` | crear (vacío) |
| 12 | `src/core/config.py` | crear |
| 13 | `src/core/db.py` | crear |
| 14 | `src/core/security.py` | crear |
| 15 | `src/features/__init__.py` | crear (vacío) |
| 16 | `src/features/auth/__init__.py` | crear (vacío) |
| 17 | `src/features/auth/models.py` | crear |
| 18 | `src/features/auth/schemas.py` | crear |
| 19 | `src/features/auth/service.py` | crear |
| 20 | `src/features/auth/dependencies.py` | crear |
| 21 | `src/features/auth/api.py` | crear |
| 22 | `tests/__init__.py` | crear (vacío) |
| 23 | `tests/conftest.py` | crear — **ojo con `StaticPool`**, ver 5.19 |
| 24 | `tests/test_auth.py` | crear — 12 tests |
| 25 | `data/.gitkeep` | crear (la carpeta debe existir; `trueque.db` se genera y va gitignored) |

### 4.2 Frontend — modificar/crear/borrar

Todas las rutas relativas a `C:\APLICACIONES\Trueque.com\frontend\`.

| # | Ruta | Acción |
|---|---|---|
| 26 | `package.json` | modificar — agregar `openapi-fetch` (dep) y `openapi-typescript` (devDep) |
| 27 | `src/lib/api/schema.d.ts` | crear — tipos generados (NO editar a mano) |
| 28 | `src/lib/api/client.ts` | crear — instancia de `openapi-fetch` |
| 29 | `src/features/auth/services/authService.ts` | **reemplazar completo** — llamadas reales |
| 30 | `src/features/auth/schemas/authSchemas.ts` | modificar — quitar `name` de `registerSchema` |
| 31 | `src/features/auth/store/authStore.ts` | modificar — `AuthUser` sin `name`, agregar `isLoading`/`isHydrated` |
| 32 | `src/features/auth/hooks/useAuthMutations.ts` | modificar — `useLogoutMutation` nuevo |
| 33 | `src/features/auth/hooks/useCurrentUser.ts` | crear — `GET /auth/me` al montar, con TanStack Query |
| 34 | `src/features/auth/components/AuthPanel/AuthPanel.tsx` | modificar — quita campo Nombre, agrega botón Google deshabilitado |
| 35 | `src/features/auth/components/AuthPanel/AuthPanel.module.scss` | modificar — estilos del botón Google |
| 36 | `src/features/auth/components/AuthPanel/AuthPanel.test.tsx` | modificar — ajustar el test de registro (ya no hay campo Nombre) |
| 37 | `src/main.tsx` | modificar — `credentials: 'include'` ya lo maneja `openapi-fetch` (ver 5.16), sin cambios aquí en realidad — **verificar y dejar igual** |

---

## 5. Especificación por archivo

> Código **literal**: copiar tal cual. Donde dice "reemplazar el contenido completo",
> borrar lo que había y pegar esto.

### 5.1 Andamiaje inicial con `uv` (no es un archivo, es el comando que genera 1 y parte de 5)

Ver Paso 1 de la Sección 6 — `uv init` genera `pyproject.toml` con valores por defecto
que luego se editan.

### 5.2 `backend/pyproject.toml` — contenido final tras editar

```toml
[project]
name = "trueque-backend"
version = "0.1.0"
description = "Backend de MyTrueque.com — FastAPI + SQLAlchemy + SQLite"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.53.0",
    "sqlalchemy>=2.0.54",
    "alembic>=1.20.0",
    "pydantic[email]>=2.13.5",
    "pydantic-settings>=2.15.0",
    "structlog>=26.1.0",
    "itsdangerous>=2.2.0",
    "bcrypt>=5.0.0",
    "python-multipart>=0.0.32",
]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "httpx2>=2.13.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"
# Los archivos que genera `alembic init` (env.py, script.py.mako, las migraciones) usan
# `Union[...]`, imports fuera del tope y bloques sin ordenar: producen 10 errores de lint
# que no son código nuestro y que se volverían a generar en cada migración nueva (H7).
extend-exclude = ["alembic"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
pythonpath = ["."]
```

### 5.3 `backend/.env.example` — crear

```bash
# Copiar a .env y ajustar. NUNCA comitear .env (está en .gitignore).
SECRET_KEY=cambia-esto-por-un-valor-aleatorio-largo
DATABASE_URL=sqlite:///./data/trueque.db
CORS_ORIGINS=http://localhost:5173
```

### 5.4 `backend/.env` — crear

Copiar `.env.example` y generar un `SECRET_KEY` real:

```bash
SECRET_KEY=<reemplazar con la salida de: python -c "import secrets; print(secrets.token_urlsafe(48))">
DATABASE_URL=sqlite:///./data/trueque.db
CORS_ORIGINS=http://localhost:5173
```

### 5.5 `backend/.gitignore` — crear

```gitignore
.venv/
__pycache__/
*.pyc
.env
data/*.db
data/*.db-journal
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
```

### 5.6 `backend/data/.gitkeep` — crear

```
```

(archivo vacío — asegura que la carpeta `data/` exista en git aunque `*.db` esté ignorado)

### 5.7 `backend/src/core/config.py` — crear

```python
"""Configuración de la app, leída de variables de entorno (.env)."""

from functools import lru_cache

from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")

    secret_key: str
    database_url: str = "sqlite:///./data/trueque.db"
    cors_origins: str = "http://localhost:5173"

    @property
    def cors_origins_list(self) -> list[str]:
        return [origin.strip() for origin in self.cors_origins.split(",") if origin.strip()]


@lru_cache
def get_settings() -> Settings:
    # Sin `# type: ignore[call-arg]`: el plugin de mypy de pydantic entiende que los
    # campos vienen del .env, y con `strict = true` un ignore innecesario es un ERROR
    # (`unused-ignore`). La v1 lo traía y rompía `mypy src` (H8).
    return Settings()
```

### 5.8 `backend/src/core/db.py` — crear

```python
"""Motor de base de datos y sesión de SQLAlchemy (SQLite)."""

from collections.abc import Generator

from sqlalchemy import create_engine
from sqlalchemy.orm import DeclarativeBase, Session, sessionmaker

from src.core.config import get_settings

settings = get_settings()

# check_same_thread=False: SQLite + un solo archivo, requerido por FastAPI (workers de
# hilo distintos por request). No es un riesgo aquí: no hay escrituras concurrentes reales
# en un backend de desarrollo con un solo proceso uvicorn.
engine = create_engine(settings.database_url, connect_args={"check_same_thread": False})
SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)


class Base(DeclarativeBase):
    pass


def get_db() -> Generator[Session, None, None]:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

### 5.9 `backend/src/core/security.py` — crear

```python
"""Hash de contraseña (bcrypt directo) y cookie de sesión firmada (itsdangerous).

Decisión cerrada: bcrypt directo, no passlib — passlib tiene un bug sin resolver con
bcrypt>=4.1 (AttributeError sobre `__about__`), reproducido en la verificación de este
plan. bcrypt.hashpw/checkpw son las dos únicas funciones que hacen falta.
"""

import bcrypt
from itsdangerous import BadSignature, URLSafeTimedSerializer

from src.core.config import get_settings

SESSION_COOKIE_NAME = "trueque_session"
SESSION_MAX_AGE_SECONDS = 60 * 60 * 24 * 7  # 7 días

settings = get_settings()
_serializer = URLSafeTimedSerializer(settings.secret_key, salt="session")


def hash_password(raw_password: str) -> str:
    return bcrypt.hashpw(raw_password.encode("utf-8"), bcrypt.gensalt()).decode("utf-8")


def verify_password(raw_password: str, password_hash: str) -> bool:
    return bcrypt.checkpw(raw_password.encode("utf-8"), password_hash.encode("utf-8"))


def create_session_token(user_id: int) -> str:
    return _serializer.dumps({"uid": user_id})


def read_session_token(token: str) -> int | None:
    """Devuelve el user_id si el token es válido y no expiró; None si no."""
    try:
        data = _serializer.loads(token, max_age=SESSION_MAX_AGE_SECONDS)
    except BadSignature:
        return None
    uid = data.get("uid")
    return uid if isinstance(uid, int) else None
```

### 5.10 `backend/src/features/auth/models.py` — crear

```python
"""Modelo de usuario. Solo correo + contraseña + @usuario — sin nombre, sin columnas
especulativas para Google (se agregan cuando exista ese plan, no antes)."""

from sqlalchemy import String
from sqlalchemy.orm import Mapped, mapped_column

from src.core.db import Base


class User(Base):
    __tablename__ = "users"

    id: Mapped[int] = mapped_column(primary_key=True)
    email: Mapped[str] = mapped_column(String(255), unique=True, index=True)
    password_hash: Mapped[str] = mapped_column(String(255))
    handle: Mapped[str] = mapped_column(String(32), unique=True, index=True)
```

### 5.11 `backend/src/features/auth/schemas.py` — crear

```python
"""Schemas Pydantic de entrada/salida del feature auth."""

from pydantic import BaseModel, EmailStr, Field


class RegisterIn(BaseModel):
    email: EmailStr
    password: str = Field(min_length=8)


class LoginIn(BaseModel):
    email: EmailStr
    password: str


class UserOut(BaseModel):
    email: str
    handle: str

    model_config = {"from_attributes": True}
```

### 5.12 `backend/src/features/auth/service.py` — crear

```python
"""Lógica de negocio de auth: generar handle único, registrar, autenticar."""

import secrets
import string

from sqlalchemy import select
from sqlalchemy.orm import Session

from src.core.security import hash_password, verify_password
from src.features.auth.models import User
from src.features.auth.schemas import RegisterIn

_HANDLE_ALPHABET = string.ascii_lowercase + string.digits
_HANDLE_MAX_ATTEMPTS = 20


class EmailAlreadyRegisteredError(Exception):
    pass


class InvalidCredentialsError(Exception):
    pass


def generate_unique_handle(db: Session) -> str:
    """Genera un @usuario con el formato @trq-XXXX (4 chars alfanuméricos), único."""
    for _ in range(_HANDLE_MAX_ATTEMPTS):
        suffix = "".join(secrets.choice(_HANDLE_ALPHABET) for _ in range(4))
        handle = f"@trq-{suffix}"
        exists = db.scalar(select(User.id).where(User.handle == handle))
        if exists is None:
            return handle
    raise RuntimeError("no se pudo generar un @usuario único tras varios intentos")


def register_user(db: Session, payload: RegisterIn) -> User:
    email = payload.email.lower()
    already_exists = db.scalar(select(User.id).where(User.email == email))
    if already_exists is not None:
        raise EmailAlreadyRegisteredError

    user = User(
        email=email,
        password_hash=hash_password(payload.password),
        handle=generate_unique_handle(db),
    )
    db.add(user)
    db.commit()
    db.refresh(user)
    return user


def authenticate_user(db: Session, email: str, password: str) -> User:
    user = db.scalar(select(User).where(User.email == email.lower()))
    if user is None or not verify_password(password, user.password_hash):
        raise InvalidCredentialsError
    return user
```

### 5.13 `backend/src/features/auth/dependencies.py` — crear

```python
"""Dependencia de FastAPI para resolver el usuario autenticado desde la cookie."""

from typing import Annotated

from fastapi import Cookie, Depends, HTTPException, status
from sqlalchemy.orm import Session

from src.core.db import get_db
from src.core.security import SESSION_COOKIE_NAME, read_session_token
from src.features.auth.models import User

DbDep = Annotated[Session, Depends(get_db)]


def get_current_user(
    db: DbDep,
    session_token: Annotated[str | None, Cookie(alias=SESSION_COOKIE_NAME)] = None,
) -> User:
    if session_token is None:
        raise HTTPException(status.HTTP_401_UNAUTHORIZED, "No hay sesión activa.")
    user_id = read_session_token(session_token)
    if user_id is None:
        raise HTTPException(status.HTTP_401_UNAUTHORIZED, "Sesión inválida o expirada.")
    user = db.get(User, user_id)
    if user is None:
        raise HTTPException(status.HTTP_401_UNAUTHORIZED, "El usuario ya no existe.")
    return user
```

### 5.14 `backend/src/features/auth/api.py` — crear

```python
"""Endpoints de auth: registro, login, logout, sesión actual."""

from typing import Annotated

from fastapi import APIRouter, Depends, HTTPException, Response, status

from src.core.security import (
    SESSION_COOKIE_NAME,
    SESSION_MAX_AGE_SECONDS,
    create_session_token,
)
from src.features.auth.dependencies import DbDep, get_current_user
from src.features.auth.models import User
from src.features.auth.schemas import LoginIn, RegisterIn, UserOut
from src.features.auth.service import (
    EmailAlreadyRegisteredError,
    InvalidCredentialsError,
    authenticate_user,
    register_user,
)

router = APIRouter(prefix="/auth", tags=["auth"])


def _set_session_cookie(response: Response, user_id: int) -> None:
    response.set_cookie(
        key=SESSION_COOKIE_NAME,
        value=create_session_token(user_id),
        max_age=SESSION_MAX_AGE_SECONDS,
        httponly=True,
        samesite="lax",
        secure=False,  # TODO: True cuando el backend sirva por HTTPS (fuera de alcance)
    )


@router.post("/register", response_model=UserOut, status_code=status.HTTP_201_CREATED)
def register(payload: RegisterIn, response: Response, db: DbDep) -> User:
    try:
        user = register_user(db, payload)
    except EmailAlreadyRegisteredError as exc:
        raise HTTPException(status.HTTP_409_CONFLICT, "Ese correo ya está registrado.") from exc
    _set_session_cookie(response, user.id)
    return user


@router.post("/login", response_model=UserOut)
def login(payload: LoginIn, response: Response, db: DbDep) -> User:
    try:
        user = authenticate_user(db, payload.email, payload.password)
    except InvalidCredentialsError as exc:
        raise HTTPException(
            status.HTTP_401_UNAUTHORIZED, "Correo o contraseña incorrectos."
        ) from exc
    _set_session_cookie(response, user.id)
    return user


@router.post("/logout", status_code=status.HTTP_204_NO_CONTENT)
def logout(response: Response) -> None:
    response.delete_cookie(SESSION_COOKIE_NAME)


@router.get("/me", response_model=UserOut)
def me(user: Annotated[User, Depends(get_current_user)]) -> User:
    return user
```

### 5.15 `backend/src/main.py` — crear

```python
"""Punto de entrada de la API."""

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from src.core.config import get_settings
from src.features.auth.api import router as auth_router

settings = get_settings()

app = FastAPI(title="MyTrueque.com API", version="0.1.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.cors_origins_list,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(auth_router)


@app.get("/health")
def health() -> dict[str, str]:
    return {"status": "ok"}
```

### 5.16 `backend/alembic.ini` — tras `alembic init` (Paso 6), editar esta línea

Localizar:
```ini
sqlalchemy.url = driver://user:pass@localhost/dbname
```
Reemplazar por:
```ini
sqlalchemy.url = sqlite:///./data/trueque.db
```

### 5.17 `backend/alembic/env.py` — modificar

Localizar la línea:
```python
target_metadata = None
```
Reemplazar por:
```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from src.core.db import Base
from src.features.auth.models import User  # noqa: F401 — registra el modelo en Base.metadata

target_metadata = Base.metadata
```

> El import de `User` es necesario aunque no se use directamente: SQLAlchemy solo conoce
> una tabla si su clase fue importada (y por tanto registrada en `Base.metadata`) antes
> de que Alembic compare el metadata contra la BD.

### 5.18 `backend/alembic/versions/0001_create_users.py` — crear

> Generada por `alembic revision --autogenerate` en el Paso 7 — este es el contenido
> exacto verificado. Si el autogenerate produce otro nombre de archivo/revision id,
> **no importa**, son aleatorios; lo que debe coincidir es el cuerpo de `upgrade()`.

```python
"""create users

Revision ID: 0001
Revises:
Create Date: 2026-09-21

"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

revision: str = "0001"
down_revision: str | Sequence[str] | None = None
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    op.create_table(
        "users",
        sa.Column("id", sa.Integer(), nullable=False),
        sa.Column("email", sa.String(length=255), nullable=False),
        sa.Column("password_hash", sa.String(length=255), nullable=False),
        sa.Column("handle", sa.String(length=32), nullable=False),
        sa.PrimaryKeyConstraint("id"),
    )
    op.create_index(op.f("ix_users_email"), "users", ["email"], unique=True)
    op.create_index(op.f("ix_users_handle"), "users", ["handle"], unique=True)


def downgrade() -> None:
    op.drop_index(op.f("ix_users_handle"), table_name="users")
    op.drop_index(op.f("ix_users_email"), table_name="users")
    op.drop_table("users")
```

### 5.19 `backend/tests/conftest.py` — crear

> **🔴 Hallazgo H1 — la versión ingenua de este archivo rompe 6 de los 8 tests.**
> `sqlite:///:memory:` crea una **base de datos nueva por cada conexión**. El fixture
> creaba la tabla en una conexión y la app pedía otra, que veía una BD vacía →
> `OperationalError: no such table: users` (reproducido: 6 failed, 2 passed).
> La corrección — verificada, 8/8 passed — es `poolclass=StaticPool` + `"sqlite://"`,
> que fuerza a reutilizar **una sola** conexión durante todo el test.
> **No simplificar este fixture.**

```python
"""Fixtures compartidos: BD SQLite en memoria + cliente de test."""

from collections.abc import Generator

import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import Session, sessionmaker
from sqlalchemy.pool import StaticPool

from src.core.db import Base, get_db
from src.features.auth.models import User  # noqa: F401
from src.main import app


@pytest.fixture
def client() -> Generator[TestClient, None, None]:
    # StaticPool + una sola conexión: sin esto, cada conexión a ":memory:" abre una BD
    # vacía distinta y la app no ve la tabla que creó el fixture (H1).
    engine = create_engine(
        "sqlite://",
        connect_args={"check_same_thread": False},
        poolclass=StaticPool,
    )
    TestingSessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)
    Base.metadata.create_all(engine)

    def override_get_db() -> Generator[Session, None, None]:
        db = TestingSessionLocal()
        try:
            yield db
        finally:
            db.close()

    app.dependency_overrides[get_db] = override_get_db
    yield TestClient(app)
    app.dependency_overrides.clear()
    Base.metadata.drop_all(engine)
```

### 5.20 `backend/tests/test_auth.py` — crear

> **Hallazgo H2**: la versión v1 de este archivo tenía una línea de 101 caracteres, que
> rompía `ruff check` contra el `line-length = 100` que el propio plan define. Se extrajo
> a constantes (`EMAIL`, `PASSWORD`) — además de arreglar el lint, quita la repetición.

```python
"""Tests de los 4 endpoints de auth."""

import re

from fastapi.testclient import TestClient

HANDLE_RE = re.compile(r"^@trq-[a-z0-9]{4}$")

EMAIL = "ana@correo.com"
PASSWORD = "supersegura123"
CREDENTIALS = {"email": EMAIL, "password": PASSWORD}


def test_register_devuelve_handle_unico(client: TestClient) -> None:
    r = client.post("/auth/register", json=CREDENTIALS)
    assert r.status_code == 201
    body = r.json()
    assert body["email"] == EMAIL
    assert HANDLE_RE.match(body["handle"])
    assert "trueque_session" in r.cookies


def test_register_correo_duplicado_da_409(client: TestClient) -> None:
    client.post("/auth/register", json=CREDENTIALS)
    r = client.post("/auth/register", json={"email": EMAIL, "password": "otra12345678"})
    assert r.status_code == 409


def test_register_password_corta_da_422(client: TestClient) -> None:
    r = client.post("/auth/register", json={"email": "x@x.com", "password": "corta"})
    assert r.status_code == 422


def test_register_email_invalido_da_422(client: TestClient) -> None:
    r = client.post("/auth/register", json={"email": "no-es-correo", "password": PASSWORD})
    assert r.status_code == 422


def test_login_password_incorrecta_da_401(client: TestClient) -> None:
    client.post("/auth/register", json=CREDENTIALS)
    r = client.post("/auth/login", json={"email": EMAIL, "password": "mala-password"})
    assert r.status_code == 401


def test_login_correo_inexistente_da_401(client: TestClient) -> None:
    r = client.post("/auth/login", json={"email": "nadie@correo.com", "password": PASSWORD})
    assert r.status_code == 401


def test_login_correcto_setea_cookie(client: TestClient) -> None:
    client.post("/auth/register", json=CREDENTIALS)
    r = client.post("/auth/login", json=CREDENTIALS)
    assert r.status_code == 200
    assert "trueque_session" in r.cookies


def test_email_se_normaliza_a_minusculas(client: TestClient) -> None:
    """Registrarse con mayúsculas y entrar con minúsculas debe funcionar (y viceversa)."""
    client.post("/auth/register", json={"email": "Ana@Correo.com", "password": PASSWORD})
    r = client.post("/auth/login", json=CREDENTIALS)
    assert r.status_code == 200


def test_me_sin_cookie_da_401(client: TestClient) -> None:
    r = client.get("/auth/me")
    assert r.status_code == 401


def test_me_con_cookie_devuelve_el_usuario(client: TestClient) -> None:
    client.post("/auth/register", json=CREDENTIALS)
    r = client.get("/auth/me")
    assert r.status_code == 200
    assert r.json()["email"] == EMAIL


def test_me_no_expone_el_hash_de_password(client: TestClient) -> None:
    """Regla no negociable 7: password_hash nunca sale en una respuesta."""
    client.post("/auth/register", json=CREDENTIALS)
    r = client.get("/auth/me")
    assert set(r.json().keys()) == {"email", "handle"}


def test_logout_borra_la_cookie(client: TestClient) -> None:
    client.post("/auth/register", json=CREDENTIALS)
    r = client.post("/auth/logout")
    assert r.status_code == 204
    r_me = client.get("/auth/me")
    assert r_me.status_code == 401
```

---

### 5.21 Frontend — `frontend/src/lib/api/client.ts` — crear

```ts
import createClient from 'openapi-fetch'
import type { paths } from './schema'

export const apiClient = createClient<paths>({
  baseUrl: 'http://localhost:8000',
  credentials: 'include', // manda/recibe la cookie de sesión httpOnly
})
```

### 5.22 `frontend/src/lib/api/schema.d.ts` — generado, NO escribir a mano

Se genera en el Paso 12 con:
```bash
pnpm exec openapi-typescript http://localhost:8000/openapi.json -o src/lib/api/schema.d.ts
```
(requiere el backend corriendo — ver Paso 12). El archivo resultante empieza con:
```ts
/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */
```
y expone `export interface paths { ... }` con `/auth/register`, `/auth/login`,
`/auth/me`, `/auth/logout`.

### 5.23 `frontend/src/features/auth/schemas/authSchemas.ts` — reemplazar el contenido completo

```ts
import { z } from 'zod'

const emailField = z.email({
  error: (issue) => (issue.input === '' ? 'Escribe tu correo.' : 'Ese correo no parece válido.'),
})

export const loginSchema = z.object({
  email: emailField,
  password: z.string().min(1, 'Escribe tu contraseña.'),
})

export const registerSchema = z.object({
  email: emailField,
  password: z.string().min(8, 'La contraseña necesita al menos 8 caracteres.'),
  acceptedTerms: z.literal(true, { error: 'Acepta los términos y las reglas de custodia.' }),
})

export type LoginFormValues = z.infer<typeof loginSchema>
export type RegisterFormValues = z.infer<typeof registerSchema>
```

> Único cambio respecto al schema actual: se quita el campo `name` de `registerSchema`
> (Decisión 4 de la Sección 1.4).

### 5.24 `frontend/src/features/auth/store/authStore.ts` — reemplazar el contenido completo

```ts
import { create } from 'zustand'

export interface AuthUser {
  email: string
  /** @usuario asignado por el servidor al registrarse, ej. "@trq-9k2f". */
  handle: string
}

interface AuthState {
  user: AuthUser | null
  /** true mientras se resuelve GET /auth/me al cargar la app. */
  isHydrating: boolean
  setUser: (user: AuthUser) => void
  clearUser: () => void
  setHydrated: () => void
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  isHydrating: true,
  setUser: (user) => set({ user, isHydrating: false }),
  clearUser: () => set({ user: null, isHydrating: false }),
  setHydrated: () => set({ isHydrating: false }),
}))
```

### 5.25 `frontend/src/features/auth/services/authService.ts` — reemplazar el contenido completo

> **🔴 Hallazgo H3 — `openapi-fetch` NO devuelve `{ error }` cuando la red falla: LANZA.**
> Verificado en navegador real apuntando a un puerto muerto: sale
> `TypeError: Failed to fetch` como excepción, no como valor de retorno. La v1 solo
> manejaba `error`, así que con el backend caído:
> - el usuario veía el genérico "Ocurrió un error." en vez de un mensaje útil, y
> - `fetchCurrentUser` nunca devolvía `null` → la app quedaba colgada (ver H4 en 5.27).
>
> Por eso cada llamada va envuelta en `try/catch` y el error de red se traduce a un
> mensaje accionable. `MENSAJE_SIN_CONEXION` es deliberadamente explícito: durante el
> desarrollo, "backend caído" es el fallo más frecuente y el más confuso de diagnosticar
> si la UI solo dice "Ocurrió un error".

```ts
import { apiClient } from '@/lib/api/client'
import type { LoginFormValues, RegisterFormValues } from '../schemas/authSchemas'
import type { AuthUser } from '../store/authStore'

export class ApiError extends Error {}

const MENSAJE_SIN_CONEXION =
  'No se pudo conectar con el servidor. ¿Está corriendo el backend en el puerto 8000?'

export async function register(values: RegisterFormValues): Promise<AuthUser> {
  const { data, error } = await withNetworkGuard(() =>
    apiClient.POST('/auth/register', {
      body: { email: values.email, password: values.password },
    }),
  )
  if (error) {
    throw new ApiError(errorMessage(error, 'No se pudo crear la cuenta.'))
  }
  return data
}

export async function login(values: LoginFormValues): Promise<AuthUser> {
  const { data, error } = await withNetworkGuard(() =>
    apiClient.POST('/auth/login', {
      body: { email: values.email, password: values.password },
    }),
  )
  if (error) {
    throw new ApiError(errorMessage(error, 'Correo o contraseña incorrectos.'))
  }
  return data
}

export async function logout(): Promise<void> {
  try {
    await apiClient.POST('/auth/logout')
  } catch {
    // Si el backend no responde, igual limpiamos la sesión del lado del cliente:
    // dejar al usuario "logueado" en la UI por un fallo de red sería peor.
  }
}

/**
 * Devuelve el usuario si hay sesión activa, o null si no.
 * **Nunca lanza** — ni por 401 ni por caída de red (H3/H4): quien la llama la usa para
 * decidir el estado inicial de la app, y una excepción aquí dejaría la UI en "cargando"
 * para siempre.
 */
export async function fetchCurrentUser(): Promise<AuthUser | null> {
  try {
    const { data, error } = await apiClient.GET('/auth/me')
    if (error) return null
    return data
  } catch {
    return null
  }
}

/**
 * openapi-fetch lanza TypeError si el fetch no llega a completarse (backend caído, DNS,
 * CORS bloqueado). Lo convierte en el mismo `{ data, error }` que devuelve en el resto de
 * los casos, para que quien llama tenga una sola forma que manejar.
 */
async function withNetworkGuard<T>(
  call: () => Promise<{ data?: T; error?: unknown }>,
): Promise<{ data: T; error?: unknown }> {
  try {
    const result = await call()
    return result as { data: T; error?: unknown }
  } catch {
    return { data: undefined as T, error: { detail: MENSAJE_SIN_CONEXION } }
  }
}

// El error de FastAPI trae { detail: string } (401/409) o { detail: ValidationError[] }
// (422). Solo el primero es texto presentable al usuario; el 422 cae al fallback porque
// zod ya valida lo mismo en el cliente y ese caso no debería llegar aquí.
function errorMessage(error: unknown, fallback: string): string {
  if (
    typeof error === 'object' &&
    error !== null &&
    'detail' in error &&
    typeof (error as { detail: unknown }).detail === 'string'
  ) {
    return (error as { detail: string }).detail
  }
  return fallback
}
```

### 5.26 `frontend/src/features/auth/hooks/useAuthMutations.ts` — reemplazar el contenido completo

```ts
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { login, logout, register } from '../services/authService'
import { useAuthStore } from '../store/authStore'
import type { LoginFormValues, RegisterFormValues } from '../schemas/authSchemas'

export function useLoginMutation() {
  const setUser = useAuthStore((state) => state.setUser)

  return useMutation({
    mutationFn: (values: LoginFormValues) => login(values),
    onSuccess: (user) => setUser(user),
  })
}

export function useRegisterMutation() {
  const setUser = useAuthStore((state) => state.setUser)

  return useMutation({
    mutationFn: (values: RegisterFormValues) => register(values),
    onSuccess: (user) => setUser(user),
  })
}

export function useLogoutMutation() {
  const clearUser = useAuthStore((state) => state.clearUser)
  const queryClient = useQueryClient()

  return useMutation({
    mutationFn: () => logout(),
    onSuccess: () => {
      clearUser()
      // Poner la caché de /auth/me en null en vez de `queryClient.clear()`: clear()
      // vacía la caché entera y deja la query montada sin datos, lo que dispara un
      // refetch inmediato al backend. Escribir el valor es determinista y no pega a red.
      queryClient.setQueryData(['auth', 'me'], null)
    },
  })
}
```

> **Hallazgo H6**: la v1 usaba `queryClient.clear()`. Además de borrar caché ajena al
> auth, deja la query `['auth','me']` sin datos y provoca un refetch inmediato —
> justo después de cerrar sesión. `setQueryData(..., null)` expresa la intención real.

### 5.27 `frontend/src/features/auth/hooks/useCurrentUser.ts` — crear

> **🔴 Hallazgo H4 — la versión v1 dejaba la app colgada en "cargando" para siempre.**
> Solo reaccionaba a `query.isSuccess`. Como `fetchCurrentUser` de la v1 **lanzaba** con
> el backend caído (H3), la query terminaba en `isError` y `setHydrated()` no se llamaba
> nunca → `isHydrating` quedaba en `true` indefinidamente. Ahora `fetchCurrentUser` ya no
> lanza (5.25), pero este hook además contempla `isError` explícitamente: es barato y
> elimina la clase entera de fallo.

```ts
import { useEffect } from 'react'
import { useQuery } from '@tanstack/react-query'
import { fetchCurrentUser } from '../services/authService'
import { useAuthStore } from '../store/authStore'

/**
 * Al montar la app, pregunta al backend si la cookie de sesión sigue siendo válida.
 * Así una recarga de página no deja al usuario "deslogueado" visualmente.
 */
export function useCurrentUser() {
  const setUser = useAuthStore((state) => state.setUser)
  const setHydrated = useAuthStore((state) => state.setHydrated)

  const query = useQuery({
    queryKey: ['auth', 'me'],
    queryFn: fetchCurrentUser,
    retry: false,
    staleTime: Infinity, // no re-preguntar en cada foco/montaje; login/logout ya actualizan el store
  })

  useEffect(() => {
    // isError no debería ocurrir (fetchCurrentUser no lanza), pero si ocurriera hay que
    // salir igual del estado "cargando" (H4).
    if (query.isError) {
      setHydrated()
      return
    }
    if (query.isSuccess) {
      if (query.data) setUser(query.data)
      else setHydrated()
    }
  }, [query.isError, query.isSuccess, query.data, setUser, setHydrated])

  return query
}
```

### 5.28 `frontend/src/features/auth/components/AuthPanel/AuthPanel.tsx` — reemplazar el contenido completo

```tsx
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { Button } from '@/shared/components/Button'
import { TextField } from '@/shared/components/TextField'
import { useLoginMutation, useRegisterMutation } from '../../hooks/useAuthMutations'
import {
  loginSchema,
  registerSchema,
  type LoginFormValues,
  type RegisterFormValues,
} from '../../schemas/authSchemas'
import { ApiError } from '../../services/authService'
import styles from './AuthPanel.module.scss'

export type AuthTab = 'login' | 'register'

interface AuthPanelProps {
  tab: AuthTab
  onTabChange: (tab: AuthTab) => void
}

/**
 * Los errores de `authService` ya vienen con un mensaje presentable (incluido el de
 * "backend caído"). Solo se cae al genérico si llega algo que no es Error — no descartar
 * `error.message` de un Error cualquiera, que era el bug H5 de la v1.
 */
function mensajeDeError(error: unknown): string {
  if (error instanceof ApiError) return error.message
  if (error instanceof Error && error.message) return error.message
  return 'Ocurrió un error. Inténtalo de nuevo.'
}

export function AuthPanel({ tab, onTabChange }: AuthPanelProps) {
  const isLogin = tab === 'login'

  return (
    <div id="acceso" className={styles.panel}>
      <div className={styles.tabs} role="tablist" aria-label="Acceso">
        <button
          type="button"
          role="tab"
          aria-selected={isLogin}
          className={isLogin ? `${styles.tab} ${styles.tabActive}` : styles.tab}
          onClick={() => onTabChange('login')}
        >
          Entrar
        </button>
        <button
          type="button"
          role="tab"
          aria-selected={!isLogin}
          className={!isLogin ? `${styles.tab} ${styles.tabActive}` : styles.tab}
          onClick={() => onTabChange('register')}
        >
          Crear cuenta
        </button>
      </div>

      {isLogin ? (
        <LoginForm onSwitch={() => onTabChange('register')} />
      ) : (
        <RegisterForm onSwitch={() => onTabChange('login')} />
      )}
    </div>
  )
}

/** Botón de Google, deshabilitado hasta tener credenciales OAuth reales. */
function GoogleButton() {
  return (
    <button type="button" className={styles.googleButton} disabled title="Próximamente">
      <span className={styles.googleIcon} aria-hidden="true">
        G
      </span>
      Continuar con Google
      <span className={styles.googleSoon}>Próximamente</span>
    </button>
  )
}

function Divider() {
  return (
    <div className={styles.divider}>
      <span>o</span>
    </div>
  )
}

function LoginForm({ onSwitch }: { onSwitch: () => void }) {
  const mutation = useLoginMutation()
  const {
    register: registerField,
    handleSubmit,
    formState: { errors },
  } = useForm<LoginFormValues>({
    resolver: zodResolver(loginSchema),
    mode: 'onBlur',
  })

  return (
    <div className={styles.body}>
      <h2 className={styles.title}>Bienvenido de vuelta</h2>
      <p className={styles.subtitle}>Entra para ver tus órdenes, liberaciones y descargas.</p>

      <form className={styles.form} onSubmit={handleSubmit((values) => mutation.mutate(values))} noValidate>
        <TextField
          label="Correo"
          type="email"
          placeholder="tu@correo.com"
          autoComplete="email"
          error={errors.email?.message}
          {...registerField('email')}
        />
        <TextField
          label="Contraseña"
          type="password"
          placeholder="••••••••"
          autoComplete="current-password"
          labelAction={
            <a href="#preguntas" className={styles.labelLink}>
              ¿La olvidaste?
            </a>
          }
          error={errors.password?.message}
          {...registerField('password')}
        />
        {mutation.isError && (
          <p className={styles.formError} role="alert">
            {mensajeDeError(mutation.error)}
          </p>
        )}
        <Button type="submit" variant="primary" size="lg" fullWidth disabled={mutation.isPending}>
          {mutation.isPending ? 'Entrando…' : 'Entrar'}
        </Button>
      </form>

      <Divider />
      <GoogleButton />

      <p className={styles.foot}>
        ¿Primera vez aquí?{' '}
        <button type="button" className={styles.footLink} onClick={onSwitch}>
          Crea tu cuenta
        </button>
      </p>
    </div>
  )
}

function RegisterForm({ onSwitch }: { onSwitch: () => void }) {
  const mutation = useRegisterMutation()
  const {
    register: registerField,
    handleSubmit,
    formState: { errors },
  } = useForm<RegisterFormValues>({
    resolver: zodResolver(registerSchema),
    mode: 'onBlur',
  })

  if (mutation.isSuccess) {
    return (
      <div className={styles.body}>
        <h2 className={styles.title}>Tu cuenta está lista</h2>
        <p className={styles.subtitle}>Este es tu @usuario. Es lo único que compartes para comprar o vender.</p>
        <div className={styles.handleBox}>
          <span className={styles.handleLabel}>TU @USUARIO</span>
          <span className={styles.handleValue}>{mutation.data.handle}</span>
        </div>
        <p className={styles.foot}>
          <button type="button" className={styles.footLink} onClick={onSwitch}>
            Ir a Entrar
          </button>
        </p>
      </div>
    )
  }

  return (
    <div className={styles.body}>
      <h2 className={styles.title}>Crea tu cuenta</h2>
      <p className={styles.subtitle}>
        Al registrarte recibes tu @usuario: es lo único que compartes para comprar o vender.
      </p>

      <form className={styles.form} onSubmit={handleSubmit((values) => mutation.mutate(values))} noValidate>
        <TextField
          label="Correo"
          type="email"
          placeholder="tu@correo.com"
          autoComplete="email"
          error={errors.email?.message}
          {...registerField('email')}
        />
        <TextField
          label="Contraseña"
          type="password"
          placeholder="Mínimo 8 caracteres"
          autoComplete="new-password"
          error={errors.password?.message}
          {...registerField('password')}
        />

        <div>
          <label className={styles.checkboxRow}>
            <input type="checkbox" className={styles.checkbox} {...registerField('acceptedTerms')} />
            <span>
              Acepto los <a href="#preguntas">términos</a> y las{' '}
              <a href="#garantias">reglas de custodia</a>.
            </span>
          </label>
          {errors.acceptedTerms && (
            <span className={styles.checkboxError} role="alert">
              {errors.acceptedTerms.message}
            </span>
          )}
        </div>

        {mutation.isError && (
          <p className={styles.formError} role="alert">
            {mensajeDeError(mutation.error)}
          </p>
        )}

        <Button type="submit" variant="primary" size="lg" fullWidth disabled={mutation.isPending}>
          {mutation.isPending ? 'Creando…' : 'Crear cuenta'}
        </Button>
      </form>

      <Divider />
      <GoogleButton />

      <p className={styles.foot}>
        ¿Ya tienes cuenta?{' '}
        <button type="button" className={styles.footLink} onClick={onSwitch}>
          Entrar
        </button>
      </p>
    </div>
  )
}
```

> **Cambios respecto al `AuthPanel.tsx` actual**: se quita el campo "Nombre" del
> `RegisterForm`; se agrega manejo de error de mutación (`mutation.isError`, no existía
> porque el servicio simulado nunca fallaba); se agregan `Divider` y `GoogleButton`.

### 5.29 `frontend/src/features/auth/components/AuthPanel/AuthPanel.module.scss` — agregar al final del archivo existente

**No reemplazar el archivo** — son estilos nuevos que se **añaden** al final del
`AuthPanel.module.scss` que ya existe (conservar todo lo que ya está):

```scss
.formError {
  font-size: 12.5px;
  color: var(--c-alert);
  margin: 0;
}

.divider {
  display: flex;
  align-items: center;
  gap: 10px;
  margin: 18px 0;
  color: var(--c-text-muted);
  font-size: 12px;

  &::before,
  &::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--c-border-inner);
  }
}

.googleButton {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  border: 1px solid var(--c-border-input);
  background: var(--c-surface);
  color: var(--c-text-muted);
  padding: 11px 13px;
  border-radius: var(--r-input);
  font-size: 14px;
  font-weight: 500;
  cursor: not-allowed;
  position: relative;
}

.googleIcon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: var(--c-border-inner);
  color: var(--c-text-muted);
  font-size: 11px;
  font-weight: 700;
  flex: none;
}

.googleSoon {
  position: absolute;
  right: 13px;
  font-family: var(--font-mono);
  font-size: 9.5px;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--c-text-placeholder);
}
```

### 5.30 `frontend/src/features/auth/components/AuthPanel/AuthPanel.test.tsx` — reemplazar el contenido completo

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { AuthPanel, type AuthTab } from './AuthPanel'

function renderPanel(tab: AuthTab = 'login', onTabChange = vi.fn()) {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  render(
    <QueryClientProvider client={client}>
      <AuthPanel tab={tab} onTabChange={onTabChange} />
    </QueryClientProvider>,
  )
  return { onTabChange }
}

describe('AuthPanel', () => {
  it('muestra el formulario de login cuando la pestaña es "login"', () => {
    renderPanel('login')
    expect(screen.getByRole('heading', { name: 'Bienvenido de vuelta' })).toBeInTheDocument()
  })

  it('muestra el formulario de registro (solo correo y contraseña) cuando la pestaña es "register"', () => {
    renderPanel('register')
    expect(screen.getByRole('heading', { name: 'Crea tu cuenta' })).toBeInTheDocument()
    expect(screen.queryByLabelText(/Nombre/)).not.toBeInTheDocument()
    expect(screen.getByLabelText(/Correo/)).toBeInTheDocument()
    expect(screen.getByLabelText(/Contraseña/)).toBeInTheDocument()
  })

  it('avisa al padre cuando se pulsa la otra pestaña', async () => {
    const user = userEvent.setup()
    const { onTabChange } = renderPanel('login')
    await user.click(screen.getByRole('tab', { name: 'Crear cuenta' }))
    expect(onTabChange).toHaveBeenCalledWith('register')
  })

  it('muestra error de validación si el correo no es válido', async () => {
    const user = userEvent.setup()
    renderPanel('login')
    await user.type(screen.getByLabelText(/Correo/), 'no-es-un-correo')
    await user.type(screen.getByLabelText(/Contraseña/), 'algo')
    await user.click(screen.getByRole('button', { name: 'Entrar' }))
    expect(await screen.findByText('Ese correo no parece válido.')).toBeInTheDocument()
  })

  it('el botón de Google está deshabilitado', () => {
    renderPanel('login')
    expect(screen.getByRole('button', { name: /Continuar con Google/ })).toBeDisabled()
  })
})
```

> **Cambio respecto al test actual**: el segundo `it` ya no verifica
> `getByLabelText(/Nombre/)` (el campo no existe); se agrega el test del botón de Google.

---

## 6. Orden de ejecución

Ejecutar en este orden exacto. Reportar cada paso como `✅ Paso N — OK` o
`❌ Paso N — FALLO: razón` y **detenerse** en el primer fallo.

### Paso 1 — Prerequisitos
Correr los 8 checks de la Sección 3.

### Paso 2 — Andamiaje del backend con uv
Desde `C:\APLICACIONES\Trueque.com`:
```bash
mkdir backend
cd backend
uv init --python 3.12 --no-readme --bare .
```
Reemplazar el `pyproject.toml` generado con el contenido exacto de la Sección 5.2.

### Paso 3 — Instalar dependencias
Dentro de `backend/`:
```bash
uv sync
```
Debe crear `.venv/` y `uv.lock` sin errores.

### Paso 4 — Configuración y estructura
1. Crear `.env.example` (5.3).
2. Crear `.env` (5.4) — generar el `SECRET_KEY` real con:
   `uv run python -c "import secrets; print(secrets.token_urlsafe(48))"`
3. Crear `.gitignore` (5.5).
4. Crear la carpeta `data/` con `data/.gitkeep` (5.6).
5. Crear `src/__init__.py`, `src/core/__init__.py`, `src/features/__init__.py`,
   `src/features/auth/__init__.py` — los 4 vacíos.

### Paso 5 — Código del core
Crear, en este orden: `src/core/config.py` (5.7), `src/core/db.py` (5.8),
`src/core/security.py` (5.9).

### Paso 6 — Feature auth
Crear, en este orden: `src/features/auth/models.py` (5.10),
`src/features/auth/schemas.py` (5.11), `src/features/auth/service.py` (5.12),
`src/features/auth/dependencies.py` (5.13), `src/features/auth/api.py` (5.14).

### Paso 7 — Entrada de la app
Crear `src/main.py` (5.15).

### Paso 8 — Alembic
1. `uv run alembic init -t generic alembic` (genera `alembic.ini` y `alembic/`).
2. Editar `alembic.ini` según 5.16.
3. Editar `alembic/env.py` según 5.17.
4. Generar la migración:
   ```bash
   uv run alembic revision --autogenerate -m "create users"
   ```
5. **Verificar** que el `upgrade()` generado coincide en sustancia con 5.18 (crea tabla
   `users` con `id`, `email`, `password_hash`, `handle`, y 2 índices únicos). Si el nombre
   de archivo/revision id difieren, no pasa nada — son aleatorios.
6. Aplicar la migración:
   ```bash
   uv run alembic upgrade head
   ```
   Debe crear `data/trueque.db`.

### Paso 9 — Tests del backend
Crear `tests/__init__.py` (vacío), `tests/conftest.py` (5.19), `tests/test_auth.py` (5.20).
```bash
uv run pytest -q
```
Deben pasar **12/12 tests**. Se tolera 1 `DeprecationWarning` de
`anyio.abc.BlockingPortal` (viene de dentro de Starlette, no del código de este plan).

> Si aparece `OperationalError: no such table: users`, el `conftest.py` no quedó con
> `StaticPool` — releer 5.19, es exactamente el fallo que esa sección previene.

### Paso 10 — Calidad del backend
```bash
uv run ruff check .
uv run mypy src
```
Ambos en **0 errores**. `ruff` cubre `src/` y `tests/` (la carpeta `alembic/` está
excluida a propósito, ver 5.2).

### Paso 11 — Levantar el backend (para el Paso 12)

⚠️ **Ejecutar siempre desde `backend/`**: `DATABASE_URL` es una ruta relativa
(`sqlite:///./data/trueque.db`), así que lanzar uvicorn desde otro directorio crearía una
BD vacía en otro lado y el login fallaría de forma desconcertante.

```bash
uv run uvicorn src.main:app --port 8000 &
```
(o en una terminal separada, sin `&`, si el entorno no soporta background jobs).
Esperar a que responda:
```bash
curl -sf http://localhost:8000/health
```
Debe devolver `{"status":"ok"}`.

> **Puerto 5173 ocupado**: si ya hay un `pnpm dev` corriendo de una sesión anterior, el
> Paso 17 lo reutiliza — no hace falta matarlo. Lo que **no** debe pasar es levantar un
> segundo Vite en otro puerto, porque `CORS_ORIGINS` solo permite `5173` y el navegador
> bloquearía las llamadas con un error de CORS poco obvio.

### Paso 12 — Generar tipos en el frontend
Dentro de `frontend/`:
```bash
pnpm add openapi-fetch
pnpm add -D openapi-typescript
mkdir -p src/lib/api
pnpm exec openapi-typescript http://localhost:8000/openapi.json -o src/lib/api/schema.d.ts
```
Verificar que `src/lib/api/schema.d.ts` contiene `"/auth/register"`, `"/auth/login"`,
`"/auth/me"`, `"/auth/logout"` dentro de `export interface paths`.

### Paso 13 — Cliente API del frontend
Crear `src/lib/api/client.ts` (5.21).

### Paso 14 — Feature auth del frontend
En este orden:
1. Reemplazar `src/features/auth/schemas/authSchemas.ts` (5.23).
2. Reemplazar `src/features/auth/store/authStore.ts` (5.24).
3. Reemplazar `src/features/auth/services/authService.ts` (5.25).
4. Reemplazar `src/features/auth/hooks/useAuthMutations.ts` (5.26).
5. Crear `src/features/auth/hooks/useCurrentUser.ts` (5.27).
6. Reemplazar `src/features/auth/components/AuthPanel/AuthPanel.tsx` (5.28).
7. Añadir al final de `AuthPanel.module.scss` los estilos de 5.29 (**no** reemplazar el
   archivo completo).
8. Reemplazar `src/features/auth/components/AuthPanel/AuthPanel.test.tsx` (5.30).

### Paso 15 — Cablear `useCurrentUser` en la app
Abrir `src/features/landing/pages/MainPage.tsx`. Al inicio del componente `MainPage`,
llamar el hook (basta con invocarlo, no hace falta usar su valor de retorno ahí — el
store de Zustand ya queda actualizado):
```tsx
import { useCurrentUser } from '@/features/auth/hooks/useCurrentUser'
```
y dentro de `export function MainPage() {`, como primera línea:
```tsx
useCurrentUser()
```

### Paso 16 — Validaciones automáticas del frontend
Dentro de `frontend/`:
```bash
pnpm typecheck
pnpm lint
pnpm test
pnpm build
```
Los 4 en verde. Si `pnpm test` falla, corregir **solo** lo que el error señale (mismo
criterio que el plan MAIN: no refactorizar por iniciativa propia).

### Paso 17 — Verificación funcional en el navegador
Con el backend corriendo (Paso 11) y `pnpm dev` corriendo en el frontend:
1. Ir a `http://localhost:5173`, pestaña "Crear cuenta".
2. Registrar `prueba@correo.com` / `contraseña123`. Debe aparecer el `@usuario` asignado.
3. Recargar la página (F5). El usuario debe seguir "logueado" (verificar en React
   DevTools o con un `console.log` temporal que `useAuthStore.getState().user` no es
   `null` — quitar el log antes de terminar).
4. Cerrar sesión (no hay botón de logout en la UI todavía — verificar vía
   `useAuthStore.getState().clearUser()` desde la consola del navegador, o llamando
   `fetch('http://localhost:8000/auth/logout', {method:'POST', credentials:'include'})`).
5. Volver a entrar con las mismas credenciales — debe funcionar.
6. Intentar registrar el mismo correo otra vez — debe mostrar el mensaje de error "Ese
   correo ya está registrado." bajo el formulario.
7. Intentar entrar con una contraseña incorrecta — debe mostrar "Correo o contraseña
   incorrectos."
8. Verificar que el botón "Continuar con Google" está visible pero no se puede pulsar.

### Paso 18 — Detener procesos
Detener el backend (`uvicorn`) y el frontend (`pnpm dev`) si quedaron corriendo en
background.

### Paso 19 — Reporte final
Reportar: archivos creados/modificados, resultado de los 19 checks (Sección 8), y
preguntar: **"El proyecto no tiene repositorio git. ¿Lo inicializo y hago el primer
commit?"** No ejecutar `git init` sin autorización explícita. **No editar `BITACORA.md`**
— la fila la agrega el dueño del proyecto al aceptar la entrega.

---

## 7. Reglas no negociables

1. **No agregar el campo "Nombre" de vuelta al registro.** Fue decisión explícita del
   usuario: "datos mínimos".
2. **No implementar el login con Google en este plan.** Solo el botón deshabilitado. No
   instalar `authlib`, `google-auth`, ni ninguna librería OAuth.
3. **No usar `passlib`.** Bcrypt directo (Decisión 1, Sección 1.4).
4. **No usar JWT.** La sesión es una cookie firmada con `itsdangerous` — ya está en el
   Stack de `CLAUDE.md`, no hay que traer `python-jose` ni similares.
5. **No usar `fetch` a pelo en el frontend para las llamadas de auth.** Siempre
   `openapi-fetch` vía `apiClient` (5.21), con los tipos generados. No escribir tipos de
   request/response a mano.
6. **`src/lib/api/schema.d.ts` no se edita a mano.** Si cambia el backend, se regenera
   con el comando del Paso 12.
7. **No exponer `password_hash` en ninguna respuesta.** `UserOut` (5.11) solo tiene
   `email` y `handle` — verificar que ningún endpoint devuelva el modelo `User` completo
   sin pasar por `UserOut`.
8. **La cookie de sesión es `httpOnly`.** El frontend nunca debe leer/escribir la cookie
   directamente vía `document.cookie` — toda la sesión se resuelve preguntándole al
   backend (`GET /auth/me`).
9. **No crear tablas ni columnas fuera de `users`** (ni `google_id`, ni `sessions`, ni
   `refresh_tokens`) — Decisión 5, Sección 1.4.
10. **No tocar `frontend/src/features/landing/`** más allá de la única línea del Paso 15
    en `MainPage.tsx`. Las 7 secciones de la landing no cambian.
11. **No crear el `backend/README.md`, CI, Docker ni nada de despliegue** — fuera de
    alcance (Sección 9).
12. **No ejecutar `git init`** sin autorización explícita (Paso 19).
13. Si algo no está especificado aquí, **preguntar antes de decidir**.

---

## 8. Validaciones post-ejecución

### Backend (desde `backend/`)

| # | Check | Comando | Resultado esperado |
|---|---|---|---|
| B1 | Dependencias instaladas | `uv sync` | exit 0, sin errores |
| B2 | Migración aplicada | `uv run alembic current` | muestra un id seguido de `(head)` |
| B3 | Tests | `uv run pytest -q` | **12 passed** (se tolera el warning de `anyio.abc.BlockingPortal`) |
| B4 | Lint | `uv run ruff check .` | `All checks passed!` |
| B5 | Types | `uv run mypy src` | `Success: no issues found in 13 source files` |
| B6 | El servidor arranca | desde `backend/`: `uv run uvicorn src.main:app --port 8000` + `curl -sf http://localhost:8000/health` | `{"status":"ok"}` |
| B7 | OpenAPI expone los 4 endpoints | `curl -s http://localhost:8000/openapi.json` | contiene `/auth/register`, `/auth/login`, `/auth/logout`, `/auth/me` |
| B8 | La BD quedó en el sitio correcto | `dir backend\data\trueque.db` | el archivo existe (si está en otro lado, uvicorn se lanzó desde el directorio equivocado — ver Paso 11) |

### Frontend (desde `frontend/`)

| # | Check | Comando | Resultado esperado |
|---|---|---|---|
| F1 | Tipos generados correctamente | revisar `src/lib/api/schema.d.ts` | contiene los 4 endpoints en `paths` |
| F2 | typecheck | `pnpm typecheck` | exit 0 |
| F3 | lint | `pnpm lint` | exit 0 |
| F4 | tests | `pnpm test` | **7 passed** (5 de `AuthPanel` + 2 de `Inspection`). Hoy son 6; el `AuthPanel.test.tsx` pasa de 4 a 5 casos |
| F5 | build | `pnpm build` | exit 0, genera `dist/` |

### Funcional (navegador, backend + frontend corriendo)

| # | Check | Resultado esperado |
|---|---|---|
| V1 | Registro con correo nuevo | Muestra el `@usuario` asignado por el backend (formato `@trq-xxxx`) |
| V2 | Recargar página tras registrarse/entrar | El usuario sigue autenticado (sesión persiste vía cookie) |
| V3 | Registro con correo repetido | Error "Ese correo ya está registrado." bajo el formulario |
| V4 | Login con contraseña incorrecta | Error "Correo o contraseña incorrectos." bajo el formulario |
| V5 | Login con las credenciales correctas tras logout | Vuelve a entrar sin problema |
| V6 | Botón "Continuar con Google" | Visible, deshabilitado, con "Próximamente" |
| V7 | **Backend caído** — detener uvicorn e intentar entrar | Mensaje "No se pudo conectar con el servidor. ¿Está corriendo el backend en el puerto 8000?" — **no** un genérico "Ocurrió un error". Verifica H3 |
| V8 | **Backend caído al cargar la página** — detener uvicorn y recargar `/` | La landing carga normal y el panel de auth es usable; la app **no** se queda en estado de carga. Verifica H4 |
| V9 | Mayúsculas en el correo | Registrarse con `Ana@Correo.com` y entrar con `ana@correo.com` funciona |

---

## 9. Fuera de alcance

1. **Login con Google real.** Solo el botón deshabilitado en la UI.
2. **Recuperar contraseña** ("¿La olvidaste?" ya existe como link en el diseño pero no
   tiene funcionalidad — apunta a `#preguntas`, sin cambios).
3. **Perfil de usuario, edición de datos, avatar.**
4. **Rutas protegidas / redirección tras login** — la landing sigue siendo la única
   página (`react-router-dom` solo tiene `/`).
5. **Botón de logout visible en la UI** — la Sección 5 deja `useLogoutMutation` listo
   para usarse, pero cablearlo a un botón real (y decidir dónde va en el header) es
   trabajo de UI que no se pidió en este plan.
6. **HTTPS / `secure=True` en la cookie** — es de despliegue, no de desarrollo local.
7. **Rate limiting, captcha, verificación de correo por email.**
8. **CI/CD, Docker, `backend/README.md`.**
9. **`git init` y commit** — se pregunta al final, no se ejecuta sin autorización.
10. **Migrar `BITACORA.md`** — la fila la agrega el dueño del proyecto.

---

## 10. Auditoría v1 → v2 — hallazgos integrados

**Método.** No fue una relectura: se levantó el backend completo con la estructura exacta
del plan (`src/core/`, `src/features/auth/`, Alembic, `.env`), se montó un frontend con
`openapi-fetch` y los tipos generados contra el OpenAPI vivo, y se ejercitó el flujo
entero en **Chromium real** cruzando origen (`localhost:5174` → `localhost:8000`) para
que la cookie `httpOnly` y CORS se comportaran como en producción de desarrollo. Después
se corrió `pytest`, `ruff`, `mypy --strict` y `tsc --noEmit` sobre el código literal del
plan.

**Sobre "pipelines configurados"**: el proyecto sigue sin CI, sin Docker y sin repositorio
git. Los únicos pipelines reales son los scripts de `frontend/package.json`
(`typecheck`/`lint`/`test`/`build`) y este plan los respeta: no toca `.oxlintrc.json`,
`tsconfig.*.json` ni `vite.config.ts`, y el `schema.d.ts` generado entra bajo `src/`, que
ya está en el `include` del tsconfig. El contrato con el frontend existente se mantiene:
`AuthPanel` conserva su API de props (`tab`/`onTabChange`), así que `Hero` y `MainPage`
no cambian salvo la línea del Paso 15.

| # | Severidad | Hallazgo | Cómo se integró |
|---|---|---|---|
| H1 | 🔴 Bloqueante | **6 de los 8 tests del backend fallaban.** `sqlite:///:memory:` abre una BD nueva por conexión: el fixture creaba la tabla y la app veía otra BD vacía → `OperationalError: no such table: users` (reproducido). | `conftest.py` reescrito con `poolclass=StaticPool` y `"sqlite://"`; el fixture `db_session` intermedio se elimina. Verificado 12/12 (5.19) |
| H2 | 🟠 Alto | `ruff check` fallaba por una línea de 101 caracteres en `test_auth.py`, contra el `line-length = 100` que el propio plan fija. | Credenciales extraídas a constantes `EMAIL`/`PASSWORD`/`CREDENTIALS`; de paso quita la repetición en 12 tests (5.20) |
| H3 | 🔴 Bloqueante | **`openapi-fetch` lanza `TypeError: Failed to fetch` con el backend caído**, no devuelve `{ error }` — reproducido en navegador. La v1 solo manejaba el valor de retorno, así que el fallo más común en desarrollo mostraba "Ocurrió un error." | `withNetworkGuard()` convierte la excepción en `{ error: { detail } }` con un mensaje accionable; `logout` y `fetchCurrentUser` con `try/catch` propio. Verificado en Chromium (5.25) |
| H4 | 🔴 Bloqueante | Consecuencia de H3: `fetchCurrentUser` lanzaba → la query terminaba en `isError` → `setHydrated()` nunca corría → **`isHydrating` quedaba `true` para siempre** con el backend caído. | `fetchCurrentUser` ya no lanza, y `useCurrentUser` maneja `isError` explícitamente. Nuevo check funcional V8 (5.27) |
| H5 | 🟡 Medio | `mutation.error instanceof ApiError ? … : 'Ocurrió un error.'` descartaba el mensaje de cualquier `Error` que no fuera `ApiError` — incluido el de red antes de H3. | Helper `mensajeDeError()` que respeta `error.message` de cualquier `Error` (5.28) |
| H6 | 🟡 Medio | `queryClient.clear()` en el logout vacía la caché entera y deja `['auth','me']` sin datos, disparando un refetch inmediato justo tras cerrar sesión. | `queryClient.setQueryData(['auth','me'], null)`, que es la intención real (5.26) |
| H7 | 🟠 Alto | `ruff check .` daba **10 errores** de archivos generados por `alembic init` (`Union[...]`, `E402`, imports sin ordenar) — ninguno código nuestro, y se regenerarían con cada migración. | `extend-exclude = ["alembic"]` en `[tool.ruff]` (5.2) |
| H8 | 🟠 Alto | `mypy src` fallaba con `Unused "type: ignore" comment [unused-ignore]`: con `strict = true`, el `# type: ignore[call-arg]` que traía `get_settings()` es un error. | Se quita el comentario; el plugin de pydantic ya resuelve el tipo (5.7) |

**Mejoras incorporadas además de los fallos:**

- Tests del backend de 8 → **12**, cubriendo dos reglas que el plan declara pero no
  verificaba: la normalización del correo a minúsculas y que `password_hash` nunca
  aparezca en una respuesta (Regla 7).
- `staleTime: Infinity` en `useCurrentUser`: sin esto, TanStack Query re-consulta
  `/auth/me` en cada montaje; el estado ya lo mantienen `login`/`logout`.
- Advertencia operativa en el Paso 11: lanzar uvicorn fuera de `backend/` crea la SQLite
  en otro directorio (la ruta es relativa) y produce un fallo de login desconcertante.
  Añadido el check B8 para detectarlo.
- Nota sobre el puerto 5173 ya ocupado por un `pnpm dev` previo: reutilizarlo, no abrir
  un segundo Vite en otro puerto (CORS solo permite 5173).

---

## Resumen ejecutivo

Backend FastAPI+SQLite con registro/login por correo y contraseña, sesión por cookie
firmada, y el frontend conectado vía `openapi-fetch` con tipos reales — sin Google
todavía, botón visible deshabilitado.
