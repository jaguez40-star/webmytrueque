# Bitácora — Trueque.com

> Historial cronológico de sesiones de trabajo. Ver `CLAUDE.md` Sección 3 para
> las reglas de diligenciamiento (una fila por tarea cerrada, solo se agrega,
> nunca se edita ni se borra una fila pasada).

---

| Fecha | ID | Descripción del cambio | Archivos principales | Commits |
|-------|----|-------------------------|-----------------------|---------|
| 2026-09-02 | SETUP | Inicialización de `CLAUDE.md` para Trueque.com a partir de un `CLAUDE.md` heredado del proyecto POLA/Dispola: retirado todo el contexto de negocio, esquema de BD, infraestructura AWS y bitácora del proyecto anterior, dejando solo idioma/estilo de respuesta y la directiva `plan:` (Sección 0.2). Agregado el Stack Tecnológico (FastAPI+SQLAlchemy backend / React 19+TS+Vite frontend, tomado de `m_CLAUDE.md`, **sin LDAP** — login contra BD propia, proyecto personal no corporativo). Documentada la arquitectura de carpetas (`backend/` y `frontend/` como hermanas en la raíz, `BITACORA.md` en archivo propio) confirmando que coincide con la referencia `Repo ProdIA`. Reseteado `BITACORA.md` (tenía el historial completo de POLA) y vaciada `Planes/` (31 archivos del proyecto viejo). | `CLAUDE.md`, `BITACORA.md`, `Planes/` | sin commit (no hay repo git) |
| 2026-09-02 | MAIN | Analizado y documentado en `CLAUDE.md` (Sección 4) el handoff de diseño `Diseño MyTrueque.com P2P/design_handoff_mytrueque_main/` — landing pública + panel de auth de MyTrueque.com, marketplace P2P de archivos con custodia/escrow. Fuente de verdad: `MyTrueque Main v5.dc.html`. Capturadas las reglas de negocio (relación 1 a 1 por `@usuario`, custodia solo mientras vive la orden, el dinero nunca pasa por la plataforma, liberación automática a las 24h, máquina de estados `EN CUSTODIA→EN INSPECCIÓN→PAGO ENVIADO→LIBERADO→DESCARGADO→PURGADO`), tokens de marca (violeta/lima OKLCH, Instrument Sans + IBM Plex Mono) y los gaps que el diseño deja abiertos (estados de error/carga de formulario, pantallas posteriores al login). | `CLAUDE.md` (Sección 4) | sin commit |
| 2026-09-02 | MAIN | Generado `Planes/plan_MAIN_20260902.md` (directiva `plan:`) con la especificación completa para scaffoldear `frontend/` desde cero e implementar la landing. La v1 asumía una plantilla de `create-vite` desactualizada (ESLint, Vite 6, TS 5). Auditada contra la plantilla real generada en un directorio temporal + versiones vigentes del registry + prueba de humo end-to-end (`tsc -b`, `vitest`, `vite build`, `oxlint`), con 12 hallazgos integrados en la v2 — 5 bloqueantes/altos: `package.json` de la v1 incompatible con Vite 8/oxlint reales, `zod` bajado a 3.x innecesariamente (`@hookform/resolvers` 5 sí soporta zod 4), `jsdom` sin pin resolvía a 30.x (no arranca en Node 20), `baseUrl` deprecado en TS 6 + el tsconfig es JSONC (no parseable como JSON), y `UploadCloud` no existe en `lucide-react` 1.x. | `Planes/plan_MAIN_20260902.md` | sin commit |
| 2026-09-02 | MAIN | Ejecutado el plan v2 como EXECUTOR, paso a paso. Scaffold de `frontend/` (Vite 8.2 + React 19.2 + TypeScript 6.0 + oxlint), 43 archivos de código creados: tokens Sass, componentes compartidos `Button`/`TextField`/`Logo`, feature `auth` (schemas zod 4 + react-hook-form + Zustand + TanStack Query, con servicio de login/registro simulado a la espera del backend), las 7 secciones de la landing como componentes React independientes, y 2 suites de test (6 tests). Dos hallazgos no anticipados por el plan, corregidos dentro del alcance de validación autorizado por el Paso 14: (1) `@testing-library/react` no limpiaba el DOM entre tests — sin `test.globals` su auto-cleanup nunca se registraba — arreglado con `afterEach(cleanup)` explícito en `src/test/setup.ts`; (2) **bug real de responsive heredado del prototipo**: ningún breakpoint del diseño original cubre el contenedor de "Entrar"/"Crear cuenta" del header, causando hasta 189px de overflow horizontal a 320px de ancho (rompía el sitio en cualquier teléfono real) — corregido ocultando ambos botones en `bp-sm` (≤560px), dejando la hamburguesa como único punto de acceso al nav en móvil angosto. Verificación visual con Playwright headless (instalado ad-hoc en el scratchpad de la sesión, sin tocar `frontend/package.json`) contra el dev server real: **13/13 checks (V0–V12) en verde**. ⏳ Pendiente: inicializar git (el proyecto no tiene repositorio) — se preguntó al usuario, a la espera de autorización explícita antes de ejecutar `git init`. | `frontend/` (proyecto completo), en particular `src/features/landing/components/Header/Header.tsx`+`.module.scss`, `src/test/setup.ts` | sin commit (pendiente `git init`) |
| 2026-09-21 | AUTH | Creado `backend/` desde cero (FastAPI + SQLAlchemy 2.0 + Alembic + SQLite) e implementada la autenticación real por correo/contraseña: `POST /auth/register`, `POST /auth/login`, `POST /auth/logout`, `GET /auth/me`, `GET /health`. Decisiones de diseño no obvias: (1) **bcrypt directo en lugar de `passlib`** — passlib tiene un bug sin resolver con bcrypt≥4.1 (`AttributeError` sobre `__about__`), reproducido durante la auditoría del plan; (2) **cookie de sesión firmada con `itsdangerous`, no JWT** — no hace falta un token portable, la sesión solo viaja entre este backend y este frontend; (3) **registro con campos mínimos** (solo correo + contraseña, sin nombre), decisión del usuario; el `@usuario` (`@trq-XXXX`) se genera automáticamente y es el único identificador que se comparte; (4) `StaticPool` obligatorio en los tests — sin él cada conexión a `sqlite://` abre una BD distinta y falla con `no such table: users`. En el frontend se reemplazó el `authService` simulado del plan MAIN por llamadas reales con `openapi-fetch` + tipos generados de OpenAPI. El botón "Continuar con Google" se dejó **deshabilitado** ("Próximamente") a la espera del plan GOOGLE. ⚠️ El backend debe levantarse **siempre desde `backend/`**: `DATABASE_URL` es una ruta relativa (`sqlite:///./data/trueque.db`). 12 tests backend + 7 frontend en verde. | `backend/` (proyecto completo), `frontend/src/features/auth/services/authService.ts`, `frontend/src/lib/api/` | sin commit |
| 2026-09-21 | GOOGLE | Implementado el login con Google (OAuth 2.0 authorization code) y el botón de cerrar sesión, ejecutando `Planes/plan_GOOGLE_20260921.md` v2 al pie de la letra. Backend: `google.py` (cliente sin librería OAuth — son dos llamadas HTTP con `httpx2`, promovido de dev a runtime), `state` anti-CSRF firmado con `itsdangerous` (mismo mecanismo que la sesión, distinto `salt`), endpoints `GET /auth/google` y `GET /auth/google/callback`, y `login_or_link_google_user()` con **vinculación por correo** a cuentas existentes. Migración `3b60a7877631`: `google_id` (único, nullable) + `password_hash` pasa a nullable. Frontend: botón de Google activado como `<a>` (el flujo OAuth necesita navegación real, no `fetch`), header con `@usuario` + botón "Salir", y lectura de `?auth=error&reason=` en `MainPage`. Decisiones no obvias: (1) **`userinfo` es `openidconnect.googleapis.com/v1/userinfo`** — el `www.googleapis.com/oauth2/v3/userinfo` de los tutoriales está obsoleto, verificado contra el discovery oficial; (2) **`email_verified` es obligatorio antes de vincular** — sin esa comprobación cualquiera podría crear una cuenta de Google con el correo de otra persona y secuestrar su cuenta; (3) no se valida la firma del `id_token` (el token llega directo de Google por HTTPS autenticando con el client secret, no vía navegador); (4) el callback **redirige**, nunca devuelve JSON. Dos hallazgos de la auditoría del plan que se materializaron aquí: **HTTP 500** al intentar login por contraseña en una cuenta creada solo con Google (`bcrypt.checkpw` recibía `None`) — corregido en `authenticate_user` devolviendo el mismo 401 genérico para no filtrar qué cuentas son de Google; y el `downgrade` de la migración necesita `batch_alter_table` con `drop_index`/`drop_column` **antes** del batch (SQLite recrea la tabla). Verificado: 22 tests backend + 7 frontend, `ruff`/`mypy --strict` en 0, `pnpm typecheck/lint/test/build` en verde, ciclo de migración up→down→up sobre la BD real, las 5 ramas del callback probadas en vivo (incluida una llamada real a Google con código inválido → `google_fallo`), Google acepta el `client_id`/`redirect_uri` reales, y ningún secreto fuera de `backend/.env`. ⏳ **Pendiente: validación del flujo completo en navegador** — requiere entrar con `rr5797372@gmail.com`, único correo autorizado mientras la app esté en modo *Testing* en Google Cloud. | `backend/src/features/auth/{google,service,api,models}.py`, `backend/src/core/{config,security}.py`, `backend/alembic/versions/3b60a7877631_*.py`, `backend/tests/test_google_auth.py`, `frontend/src/features/auth/components/AuthPanel/`, `frontend/src/features/landing/components/Header/`, `frontend/src/features/landing/pages/MainPage.tsx` | sin commit |
| 2026-09-21 | GOOGLE | Cerrada la validación en navegador que había quedado pendiente de la fila anterior: login real con `rr5797372@gmail.com` completado en Chrome contra Google de verdad (cuenta creada con `google_id` propio y `password_hash` nulo, confirmado en la BD), botón "Salir" probado (vuelve a "Entrar / Crear cuenta"), y segundo login con la misma cuenta de Google reutiliza el mismo `@trq-925j` sin duplicar el usuario (confirmado: 1 solo registro en `users` tras el segundo login). Además, a pedido del usuario, se le dio más peso visual al botón de Google: el ícono pasó de una "G" gris en círculo a un SVG del logo oficial a 4 colores, borde más marcado y sombra al hover — para que se lea como una opción tan válida como "Crear cuenta", no como algo secundario o deshabilitado. Sin pendientes: el plan GOOGLE queda completo y verificado de punta a punta (backend, frontend y navegador real). | `frontend/src/features/auth/components/AuthPanel/AuthPanel.tsx`, `AuthPanel.module.scss` | sin commit |
| 2026-09-21 | PANEL | Implementado el panel post-login **mobile-first**: 5 pantallas privadas (`/panel` bandeja y estado vacío, `/panel/orden/:id`, `/panel/nueva`, `/panel/cuenta`) con routing protegido. **El diseño se rehízo desde cero**: las versiones 1 y 2 del plan partían de artboards de 1280px, pero al medir la app real a 360/390/412/430px se encontró que la landing tenía 8 objetivos táctiles por debajo de 44px (incluidos "Entrar" y "Crear cuenta", a 38px), 60+ textos bajo 14px y 5 a 9,5px — y el usuario confirmó que **el 90% del tráfico serán teléfonos de 6–7"**. Eso obligó a cambiar arquitectura, no CSS: (1) la acción principal vive en una **barra fija inferior** (en escritorio era una columna lateral que al apilarse enterraba el CTA tras 5 tarjetas); (2) el `@usuario` y "Cerrar sesión" se mudaron del header a una **pantalla de cuenta nueva**, porque a 390px el header con logo + chip + botón desbordaba 32px; (3) la bandeja se filtra con un **segmented control** en vez de dos secciones apiladas, que en teléfono eran varias pantallas de scroll. Decisiones no obvias: `useCurrentUser()` **sube a `App.tsx`** (si alguien entra directo a `/panel`, la landing nunca se monta y la sesión jamás se hidrataría); `RutaPrivada` **espera durante `isHydrating`** en vez de redirigir (sin eso, recargar `/panel` con cookie válida expulsa al usuario antes de que llegue `/auth/me`); los inputs llevan **`font-size: 16px` obligatorio** porque por debajo iOS hace zoom automático al enfocar; `viewport-fit=cover` + `env(safe-area-inset-bottom)` para que la barra fija no quede bajo el indicador de inicio del iPhone; y el hash SHA-256 se muestra **abreviado con desplegable**, porque los 64 caracteres ocupan 3 líneas en un teléfono. Se añadieron mixins `min-width` (`desde-md`/`desde-lg`) al `_mixins.scss` existente, que solo tenía `max-width` — la landing sigue usando los suyos. Las órdenes vienen de datos de ejemplo vía `useOrders()`, que es la costura para el backend futuro (`?demo=1` los activa; por defecto el panel sale vacío). Auditoría previa del plan sobre un clon encontró 2 fallos de accesibilidad táctil ya corregidos aquí (marca del header a 30px y filtro a 40px, ambos bajo el mínimo de 44). Verificado: 23/23 tests, typecheck/lint/build en verde, y **28/28 comprobaciones en Chromium real a 360px y 390px** — cero desborde horizontal, ningún toque bajo 44px, la barra fija visible sin scroll y sin tapar el pie, y cero errores de JavaScript. Landing sin regresión. ⚠️ Descubierto durante la ejecución: el repositorio git ya existía con remoto (`github.com/jaguez40-star/webmytrueque`) y 3 commits hechos fuera de las sesiones de Claude; el plan asumía que no había repo. `backend/.env` sigue correctamente ignorado. **Fuera de alcance (pendiente)**: el backend de órdenes (subida de archivos, cifrado, hash real, purga, temporizador de 24 h) — los botones de acción son maqueta; y el responsive de la landing, que conserva sus toques de 38px. | `frontend/src/features/orders/` (43 archivos), `frontend/src/features/auth/components/RutaPrivada.tsx`, `frontend/src/App.tsx`, `frontend/src/features/landing/pages/MainPage.tsx`, `frontend/src/styles/_mixins.scss`, `frontend/src/styles/_tokens.scss`, `frontend/index.html` | ver commit de esta fila |
| 2026-09-21 | MODAL | Convertido `/panel/nueva` en **modal desde 768px**, manteniéndolo a pantalla completa en teléfono, ejecutando `Planes/plan_MODAL_20260921.md` v2. La decisión de no usar modal en móvil no es estética sino medida: el formulario ocupa **1337px (1,58 pantallas a 390px)** y con el teclado abierto quedan ~456px útiles, con los inputs a 622/808/897px — dentro de un modal el campo del monto acaba tapado, además del doble scroll, del botón atrás de Android saliendo del panel entero y de la pérdida de enlace. Es el patrón de Gmail/Mail de iOS. Decisiones no obvias: (1) **`<dialog>` nativo con `showModal()`** en vez de un `<div>` con overlay — el navegador aporta atrapado de foco, Escape, `::backdrop` e `inert` sin escribir un focus trap a mano; (2) **routing con "ubicación de fondo"** (`state={{ background: location }}`), de modo que la URL no miente, el botón atrás cierra el modal y el enlace directo a `/panel/nueva` sigue abriendo la página completa; (3) **`useSyncExternalStore`** para el media query, no `useEffect`+`setState`, que dispararía `react/set-state-in-effect` de oxlint; (4) el formulario se extrajo a `FormularioNuevaOrden` para que página y modal compartan marcado. La auditoría del plan sobre un clon encontró 4 defectos ya corregidos aquí: **el foco caía en `<body>` al cerrar** (`showModal()` no lo cubre porque el modal se desmonta al navegar, no se cierra con `close()`; ahora guarda `document.activeElement` y lo restaura, coherente con lo que ya hacía `MenuCuenta`), `aria-labelledby` con id escrito a mano sustituido por `useId()`, y **dos criterios de aceptación del plan que eran falsos** (al recargar el modal SÍ sobrevive, porque el `state` vive en la entrada del historial; y el ciclo de Tab pasa por `<body>` en Chromium sin que eso sea una fuga de foco). Verificado: 38 tests, typecheck/lint/build en verde, **27/27 comprobaciones del executor + 20/20 de la verificación independiente** en Chromium real contra el backend real a 390px, 767px, 768px y 1280px — incluidos scroll chaining, paridad de campos entre modal y página, y no-regresión de `MenuCuenta` y del detalle de orden. ⚠️ **Dos efectos colaterales que el plan no anunciaba**: el botón de subida cambió de copy, de "Buscar en mi teléfono" a **"Buscar archivo"** (correcto ahora que el mismo formulario se ve en escritorio, pero es un cambio de texto visible); y los títulos de los tres pasos pasaron de `<h2>` a `<h3>` para encajar bajo el `<h2>` del modal, lo que deja la **página completa saltando de `<h1>` a `<h3>`** — hueco de jerarquía para lectores de pantalla en la vista que verá el 90% del tráfico. Se recomienda que `FormularioNuevaOrden` reciba el nivel de encabezado como prop. **Fuera de alcance**: que el formulario envíe algo (sigue siendo maqueta, no hay backend de órdenes), animaciones del modal, y convertir el detalle de orden en modal. Quedó en la BD de desarrollo una cuenta de prueba creada para la verificación en navegador: `executor-modal-1790048238571@example.com`. | `frontend/src/shared/components/Modal/`, `frontend/src/shared/hooks/useMediaQuery.ts`, `frontend/src/features/orders/components/FormularioNuevaOrden/`, `frontend/src/features/orders/components/ModalNuevaOrden/`, `frontend/src/App.tsx`, `frontend/src/features/orders/pages/NuevaOrdenPage.tsx`+`.module.scss`, `frontend/src/features/orders/pages/PanelPage.tsx` | pendiente |
| 2026-09-21 | ORDENES | Implementado el primer backend real de órdenes ejecutando `Planes/plan_ORDENES_20260921.md` v2: crear una orden sube archivos de verdad a disco con hash SHA-256 real, y ambas partes la ven en su panel. Hasta aquí todo lo demás del producto era maqueta — login era lo único vivo. **La restricción que gobernó el diseño es de infraestructura, no de producto**: el EC2 de producción tiene 6,7 GB de disco con solo ~3 GB libres, así que el tope se fijó en **1 GB por orden** (no 5 GB como pedía el diseño original) y el servidor comprueba espacio libre antes de aceptar, con 512 MB de margen siempre reservado — llenar el disco de un t3.micro no rechaza la siguiente subida, tumba el servicio entero (SQLite deja de poder escribir). Decisiones no obvias: (1) **el nombre en disco es un UUID, nunca el que mandó el usuario** — un nombre con `../` permite escribir fuera de la carpeta de la orden, y el nombre real solo vive en la BD; (2) **hash y escritura por bloques de 1 MB**, nunca el archivo completo en memoria (el servidor tiene 1 GB de RAM y el tope es 1 GB, cargarlo entero sería OOM); (3) **`Order.archivos` es una lista, no un campo singular** — el formulario ya permitía elegir varios, así que se añadió `resumenDeArchivos()` para que las vistas compactas cambien una línea en vez de rediseñarse; (4) la subida usa **`XMLHttpRequest`, no `fetch`** — es la única API con progreso de subida, y 1 GB sin barra de progreso es una pantalla congelada varios minutos; (5) el envío se coordina con un **contexto de React** porque el botón vive fuera del formulario (en el pie del modal y en la barra fija de la página). La auditoría del plan —ejecutándolo de verdad sobre una copia del proyecto— encontró **13 hallazgos, dos graves**: (H1) el tope de 1 GB **no protegía el disco de verdad** — Starlette parsea el multipart entero a un archivo temporal antes de que el endpoint pueda mirar `UploadFile.size`, así que un cuerpo de 20 MB con el tope en 1 KB tardaba **3,11 s** en rechazarse porque el servidor ya lo había leído completo; se corrigió con un middleware que rechaza por `Content-Length` **antes** de leer el cuerpo (medido: 3,11 s → 0,01 s); (H2) **dos relojes distintos en la misma fila** — `created_at` usaba `func.now()` de SQLite (UTC) y `purge_at` usaba `datetime.now()` de Python (hora local), dando 29,79 días de purga en vez de 30 y fechas sin zona horaria que el navegador leía como locales (una orden recién creada parecía estar 5 h en el futuro, y `haceCuanto()` habría dicho "recién" para todo durante esas 5 horas); se corrigió con un único reloj `ahora_utc()` y serialización con sufijo `Z` (medido: 30,000 días exactos). El resto de hallazgos: un N+1 real (listar 20 órdenes disparaba 43 consultas SQL, corregido a 6 y ya no crece con el número de órdenes), un import sin usar que rompía `ruff`, un warning de lint por exportar componente y hook del mismo archivo (el contexto se partió en dos), un test existente (`OrderCard.test.tsx`) que el plan no había detectado y rompía el typecheck, y varias imprecisiones de redacción del plan (imports de ejemplo inventados, instrucciones en prosa ambigua) reescritas como sustituciones literales. Verificado dos veces de forma independiente: 34 tests backend (ruff + mypy --strict + migración up→down→up en verde), 45 tests frontend (typecheck/lint sin warnings/build en verde), y en Chromium real contra el backend real con cuentas reales — **28/28** del executor más **13/13** de una segunda verificación con datos distintos, incluyendo comparar el hash SHA-256 devuelto por el servidor contra el calculado localmente sobre un archivo de contenido conocido (coincidencia exacta) y confirmar que `GET /orders` responde en 8 ms tras el arreglo del N+1. Ningún archivo de `backend/data/custodia/` aparece en `git status` — la regla ya estaba en `.gitignore` de una sesión anterior, verificada antes de crear nada. **Fuera de alcance (pendiente, documentado en el propio plan)**: descargar el archivo, cifrado en reposo (el copy de la UI ya lo promete, la implementación queda para la siguiente tanda), purga automática a los 30 días (`purge_at` se calcula pero nada la ejecuta todavía — hace falta una tarea programada), las transiciones de estado (`EN_INSPECCIÓN`, `PAGO_ENVIADO`, `LIBERADO`, `DESCARGADO` siguen siendo maqueta), subir el comprobante de pago, y subida reanudable. | `backend/src/core/storage.py`, `backend/src/core/config.py`, `backend/src/main.py`, `backend/src/features/orders/` (modelos, schemas, service, api), `backend/alembic/versions/a1b2c3d4e5f6_create_orders.py`, `backend/tests/test_orders.py`, `frontend/src/features/orders/types.ts`, `frontend/src/features/orders/context/`, `frontend/src/features/orders/services/ordersService.ts`, `frontend/src/features/orders/hooks/{useOrders,useCrearOrden}.ts`, `frontend/src/features/orders/components/{FormularioNuevaOrden,ModalNuevaOrden,OrderCard,GrillaArchivos}/`, `frontend/src/features/orders/pages/{NuevaOrdenPage,DetalleOrdenPage}.tsx`, `frontend/src/lib/api/schema.d.ts` | ver commit de esta fila |
| 2026-09-22 | DESCARGA | Cerrado el ciclo de la custodia: el vendedor **autoriza** y el comprador **descarga**. Hasta aquí el archivo entraba al servidor y no volvía a salir — `LIBERADO`/`DESCARGADO` eran estados de maqueta. Backend: columna `downloaded_at` (migración `b2c3d4e5f6a7`, `batch_alter_table` por SQLite, ciclo up→down→up verificado), `cambiar_autorizacion()` (`EN_CUSTODIA` ⇄ `LIBERADO`) y `archivo_para_descarga()`, expuestos como `PUT /orders/{id}/autorizacion` y `GET /orders/{id}/archivos/{archivo_id}`. Decisiones no obvias: (1) **el vendedor puede revocar, pero solo hasta la primera descarga** — después el comprador ya tiene el archivo y revocar prometería un control que no existe; el backend responde 409 y el switch se congela; (2) **404 y no 403 cuando quien pide no es el vendedor de esa orden** — un 403 confirmaría que la orden existe; (3) **descargar NO purga ni borra nada**: marca `downloaded_at` y deja los bytes, para que una descarga cortada se pueda reintentar (la purga sigue siendo la de los 30 días); (4) la autorización se manda como **estado deseado, no como toggle** — dos pestañas abiertas acaban en lo mismo en vez de cancelarse entre ellas; (5) el frontend baja por **`fetch` + blob y no por un `<a download>` directo**, porque en desarrollo el backend está en otro origen (:8000) y una navegación normal no mandaría la cookie de sesión; (6) **sin ZIP**: los archivos bajan uno por uno con su nombre original, que es lo que el comprador verifica contra el hash. UI: la tarjeta de la grilla pasó a ser el `<li>` y no el `<a>`, porque un control interactivo dentro de un enlace es HTML inválido y, en la práctica, un clic en el switch acabaría navegando al detalle. **Un defecto encontrado por la prueba en navegador, no por los tests unitarios**: el input del switch estaba oculto a 1×1 px y la pista lo tapaba, así que un clic sobre el switch no llegaba al control — solo funcionaba por rebote del `<label>`; se corrigió estirando el input encima de la pista con `opacity: 0`. Verificado: 41 tests backend (ruff + mypy --strict en verde), 56 frontend (7 nuevos, typecheck/lint/build en verde) y **18/18 comprobaciones en Chromium real con dos cuentas registradas de verdad**, incluyendo que el contenido descargado es byte a byte el subido, que el nombre original se conserva, que el botón del comprador está inactivo antes de autorizar y activo después, que el switch se congela tras la descarga, y que a 390px no hay desborde horizontal ni objetivos táctiles bajo 44px. **Fuera de alcance (sigue pendiente)**: cifrado en reposo, purga automática a los 30 días, `EN_INSPECCIÓN`/`PAGO_ENVIADO` y la subida del comprobante. | `backend/src/features/orders/{models,schemas,service,api}.py`, `backend/alembic/versions/b2c3d4e5f6a7_downloaded_at.py`, `backend/tests/test_orders.py`, `frontend/src/features/orders/components/AccionesOrden/`, `frontend/src/features/orders/components/GrillaArchivos/`, `frontend/src/features/orders/hooks/useAutorizarDescarga.ts`, `frontend/src/features/orders/services/ordersService.ts`, `frontend/src/features/orders/types.ts` | ver commit de esta fila |
| 2026-09-22 | PURGA | El vendedor puede borrar sus archivos en custodia cuando quiera. Hasta aquí lo único que sacaba bytes del disco era la purga a 30 días, que **sigue sin existir** (no hay tarea programada que la ejecute), así que en la práctica nada se borraba nunca. Backend: `purgar_orden()` y `DELETE /orders/{id}`. Decisiones no obvias: (1) **solo el vendedor** — lo que el comprador tiene en custodia no es suyo, lo tiene disponible hasta los 30 días o hasta que el vendedor purgue, así que el comprador recibe **404, no 403** (un 403 le confirmaría que la orden existe y que solo le falta permiso); (2) **se borran los bytes, no la historia**: la carpeta desaparece del disco pero las filas de `order_files` quedan, con nombre y SHA-256 — ocupan nada y `PURGADO` ya dice que los archivos no están; (3) **idempotente**: purgar dos veces devuelve 200, no un error, porque la segunda simplemente no tiene nada que borrar; (4) la orden pasa a `PURGADO`, que `estaCerrada()` ya filtraba, así que desaparece sola de las dos grillas sin tocar el filtrado; (5) en la UI **no se borra al primer clic** — la confirmación es en el sitio y dice qué se pierde ("Se borran para siempre y el comprador deja de verlos"), no un "¿seguro?"; no se usó `window.confirm`, que en teléfono roba la pantalla entera y no se puede estilar. Verificado: 47 tests backend (6 nuevos: el vendedor purga y los bytes desaparecen del disco, el comprador recibe 404 y sus archivos siguen intactos, un tercero recibe 404, tras purgar la descarga da 403, purgar dos veces no falla, y el rastro nombre+hash sobrevive), 61 frontend (4 nuevos), ruff + mypy --strict + typecheck + lint + build en verde, y **11/11 comprobaciones en Chromium con dos cuentas reales** — incluido que el comprador no ve ningún botón de borrar, que un DELETE directo a la API desde su sesión responde 404, que el primer clic no borra nada, que cancelar deja todo igual, y que al confirmar la orden desaparece de las dos grillas mientras la otra orden sigue intacta. **Pendiente (sin cambios)**: la purga automática a los 30 días sigue necesitando una tarea programada — `purge_at` se calcula y nadie lo ejecuta. | `backend/src/features/orders/{service,api}.py`, `backend/tests/test_orders.py`, `frontend/src/features/orders/components/AccionesOrden/`, `frontend/src/features/orders/hooks/usePurgarOrden.ts`, `frontend/src/features/orders/services/ordersService.ts` | ver commit de esta fila |
| 2026-09-21 | DEPLOY | Primer despliegue a producción: instancia AWS EC2 (`i-0f2619b4bbaa0dda6`, Ubuntu, t3.micro, región Ohio) con Elastic IP `18.223.51.0`, dominio `mytrueque.shop` (Hostinger) apuntando por registro A, nginx como reverse proxy (`/auth/` y `/health` al backend en `127.0.0.1:8000`, resto como SPA estática con `try_files`) + certificado SSL de Let's Encrypt (renovación automática), backend corriendo como servicio systemd (`trueque-backend`, `Restart=always`). El repo **no era git** hasta esta sesión — se inicializó y se subió a `https://github.com/jaguez40-star/webmytrueque` (rama `main`). Dos bugs reales encontrados recién en producción, invisibles en local: (1) **migración `3b60a7877631` fallaba en SQLite real** (`near "ALTER": syntax error`) — el `upgrade()` usaba `op.alter_column` directo en vez de `batch_alter_table` (el `downgrade()` sí lo usaba bien); en local nunca se había probado porque la BD de desarrollo ya tenía el esquema final por otro camino; corregido y verificado corriendo la migración desde cero contra una BD descartable; (2) **la URL del backend estaba hardcodeada a `http://localhost:8000`** en `client.ts` y en el botón de Google — funcionaba en desarrollo (mismo origin lógico) pero en producción cada visitante intentaba pegarle a *su propio* localhost: login y OAuth habrían fallado para el 100% de usuarios reales; el síntoma que lo delató fue un permiso de Chrome ("acceder a otras aplicaciones y servicios de este dispositivo") en vez de un error obvio; corregido con `import.meta.env.PROD` para usar rutas relativas en build de producción. También: `backend/.env.example` tenía `GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxx` como placeholder — el clasificador de seguridad de Claude Code bloqueó el commit porque el prefijo `GOCSPX-` es el formato real de un secret de Google; placeholders futuros van como `<algo>`, nunca con el prefijo real del proveedor. Disco del servidor: 6,7 GB totales, ~3 GB libres — dato que después fijó el tope de 1 GB por orden en la sesión ORDENES. | `.gitignore` (raíz, nuevo), `backend/.env.example`, `backend/alembic/versions/3b60a7877631_*.py`, `frontend/src/lib/api/client.ts`, `frontend/src/features/auth/components/AuthPanel/AuthPanel.tsx`, config de nginx y systemd (no versionados, viven solo en el servidor) | `fa2037d`..`8440dfb` |
| 2026-09-22 | OAUTH | Publicado el cliente OAuth de Google de *Testing* a **Producción** — hasta esta sesión solo podía entrar con Google el correo dado de alta como usuario de prueba (`rr5797372@gmail.com`); confirmado con una consulta directa a la tabla `users` en la BD de producción (vía SSH) que mostró un segundo `google_id` distinto ya logueado — resultó ser la cuenta con rol en el proyecto de Google Cloud, que Google deja pasar en *Testing* sin necesidad de estar en la lista explícita. Para publicar hizo falta completar "Información de la marca" con URIs reales de política de privacidad y términos del servicio, que no existían — se crearon `frontend/src/features/legal/` (`/privacidad`, `/terminos`) con contenido real basado en las reglas de negocio ya documentadas (custodia, purga a 30 días, dinero nunca pasa por la plataforma), no texto de relleno; enlazadas desde el footer y el checkbox de registro (que antes apuntaba a un `#preguntas` que no explicaba nada). Se subió un logo a la pantalla de consentimiento y la verificación automática de marca falló ("el sitio no está registrado a tu nombre", exige Google Search Console) — se sacó el logo para publicar sin necesitar esa verificación, que queda pendiente si en el futuro se quiere mostrar logo. Estado final: **En producción**, cualquier cuenta de Google puede registrarse. | `frontend/src/features/legal/` (nuevo), `frontend/src/features/landing/components/Footer/`, `frontend/src/features/auth/components/AuthPanel/AuthPanel.tsx`, `CLAUDE.md` (§2.1) | `18d6d4e` |
| 2026-09-22 | PURGA-1 | Borrado **por archivo**, no solo de la orden entera. Con varias ventas en una misma orden, quitar uno que sobra obligaba a purgarlo todo y volver a subir el resto. Backend: `purgar_archivo()` y `DELETE /orders/{id}/archivos/{archivo_id}`. Decisiones no obvias: (1) aquí **sí se borra la fila** de `order_files`, al revés que en el borrado total (que conserva nombre y hash con la orden en `PURGADO`) — no existe un estado "purgado" por archivo, así que dejar la fila sería mentirle a la lista que el comprador ve; (2) **si era el último archivo la orden pasa a `PURGADO`** y se borra la carpeta: una orden sin nada que custodiar no puede quedarse a medias; (3) **el id del archivo no basta** — tiene que pertenecer a la orden de la URL, o es 404 (sin esa comprobación, conocer un id permitiría borrar archivos de otra orden propia); (4) **la tarjeta de la grilla dejó de ser un `<a>` completo**: cada fila lleva ahora su papelera y un botón dentro de un enlace es HTML inválido además de un clic que navegaría; el detalle pasó a un enlace explícito "Ver detalle" en el pie, y el botón de la orden se renombró a **"Borrar todo"** para no confundirlo con la papelera de cada fila; (5) la papelera de fila usa **32px y no los 44px** del resto: va dentro de una fila de 38px y estirarla convertiría una orden de cinco archivos en media pantalla de scroll — es un icono secundario que además pide confirmación. Un hallazgo salió de la propia prueba en navegador: el botón de cancelar decía solo **"No"**, nombre accesible inútil para un lector de pantalla; se le puso `aria-label` con el nombre del archivo ("Cancelar borrado de dos.png"), igual que al de confirmar. Verificado: 52 tests backend (5 nuevos), 61 frontend, ruff + mypy --strict + typecheck + lint + build en verde, y **13/13 comprobaciones en Chromium con dos cuentas reales** — borrar uno deja los otros dos intactos y el contador pasa a "2 ARCHIVOS", al comprador le desaparece solo ese, borrar el último deja la orden en `PURGADO` y fuera de la grilla, el comprador no ve ninguna papelera y un DELETE directo desde su sesión responde 404. Se repasaron además los dos flujos anteriores (descarga 18/18, borrado total 11/11) porque el cambio de estructura de la tarjeta los tocaba. | `backend/src/core/storage.py`, `backend/src/features/orders/{service,api}.py`, `backend/tests/test_orders.py`, `frontend/src/features/orders/components/GrillaArchivos/`, `frontend/src/features/orders/components/AccionesOrden/AccionesOrden.tsx`, `frontend/src/features/orders/hooks/usePurgarOrden.ts`, `frontend/src/features/orders/services/ordersService.ts` | ver commit de esta fila |
| 2026-09-22 | ADMIN | Panel de almacenamiento en `/panel/admin`, para UNA sola cuenta: ver el disco, bajar backups y borrar archivos de cualquier usuario. Antes de escribir nada se hizo el diagnóstico de viabilidad que pidió el usuario; el hallazgo que lo gobierna todo es que **esta pantalla no da un poder nuevo —quien tiene la clave SSH ya podía leerlo todo— sino que abre una puerta mucho más fácil de forzar**: de necesitar el `.pem` a necesitar una sesión. De ahí las decisiones: (1) **el admin es un correo en `.env` (`ADMIN_EMAIL`), no una columna** — no hay roles en el modelo, es literalmente una cuenta, y una columna `is_admin` traería migración y una forma de activarla sin aportar nada; sin la variable configurada **nadie** es admin, así que un `.env` incompleto no abre la puerta; (2) **404 y no 403 en los seis endpoints**: un 403 confirmaría que el panel existe y que solo falta ser alguien concreto, justo la pista que no queremos dar sobre la única pantalla que lee los archivos de todo el mundo; la página del frontend enseña ese mismo 404, sin insinuar nada; (3) **no hay guardia de admin en el cliente** — la ruta se pide igual y el backend decide; `esAdmin` (nuevo campo de `/auth/me`) solo sirve para enseñar u ocultar el enlace del menú de cuenta; (4) **el backup ZIP se genera en streaming**, con un buffer no-seekable que `zipfile` rellena al vuelo: escribirlo antes de mandarlo duplicaría el tamaño en un disco con 2,9 GB libres y un tope de 1 GB por orden — exactamente lo que `MARGEN_DISCO_BYTES` intenta evitar; sin compresión (`ZIP_STORED`) porque lo que se guarda son PNG/ZIP/MP4 ya comprimidos y la CPU del t3.micro no está para regalar; (5) **el borrado de carpetas huérfanas lleva dos cerrojos contra `../`**: el nombre tiene que ser solo dígitos y la ruta resuelta tiene que caer dentro de custodia — sin eso el endpoint borra cualquier fichero del servidor; (6) el panel **cruza la base con el disco** y delata las dos formas de incoherencia que nadie veía: archivos con fila pero sin bytes (alguien borró por SSH) y carpetas con bytes pero sin orden (rollback fallido); (7) `purgar_orden`/`purgar_archivo` se partieron en dos para que el admin reutilice el borrado sin heredar la comprobación de "soy el vendedor". **Descartado**: re-autenticación por contraseña al entrar al panel — la cuenta admin (`jaguez40@gmail.com`) entró con Google y **no tiene contraseña**, así que no hay nada que volver a pedir. En nginx hizo falta un `location /admin` con `proxy_buffering off` y `proxy_max_temp_file_size 0`: sin eso nginx guarda el ZIP entero en un temporal antes de mandarlo y anula el streaming. Verificado: 64 tests backend (12 nuevos, incluido el intento de traversal contra un fichero señuelo fuera de custodia, que sobrevive intacto), 67 frontend (6 nuevos), ruff + mypy --strict + typecheck + lint + build en verde, y **20/20 comprobaciones en Chromium con tres cuentas reales** — una cuenta corriente no ve el enlace, la URL a mano le devuelve "Esta página no existe" y la API un 404; el admin ve la orden ajena, baja un ZIP que se abre de verdad y contiene los nombres y el contenido originales, borra un archivo suelto en dos tiempos y al vendedor le desaparece de su panel. ⚠️ **Dos hallazgos del proceso**: el `role="menuitem"` del enlace pisaba el rol nativo de `link` (quitado, y ningún otro control del menú lo lleva); y un `pkill` que no mató el uvicorn viejo hizo creer durante un rato que `ADMIN_EMAIL` no se leía — el proceso del puerto era el anterior. **Pendiente**: la purga automática a 30 días sigue sin tarea programada, y ahora hay un sitio desde donde hacerla a mano. | `backend/src/core/{config,storage}.py`, `backend/src/features/admin/` (api, service, schemas), `backend/src/features/auth/{dependencies,api,schemas}.py`, `backend/src/features/orders/service.py`, `backend/src/main.py`, `backend/tests/test_admin.py`, `backend/.env.example`, `frontend/src/features/admin/` (página, hooks, servicio), `frontend/src/features/orders/components/MenuCuenta/`, `frontend/src/features/auth/store/authStore.ts`, `frontend/src/App.tsx`, `frontend/src/lib/api/schema.d.ts` | ver commit de esta fila |
